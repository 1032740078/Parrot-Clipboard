# v0.1 冲刺任务文档

> **版本**：v1.1 · **版本范围**：v0.1 MVP · **编写日期**：2026-03-05
> **更新说明**：重构为表格格式，新增进度监控与记录功能；2026-03-06 已更新到 22/24 任务完成进度

---

## 1. 进度总览仪表盘

> 每次更新任务状态后，手动刷新下方统计数据。

### 1.1 状态图例

| 标识 | 含义 |
|------|------|
| `⬜ 待开始` | 尚未开始 |
| `🔄 进行中` | 正在开发 |
| `✅ 已完成` | 开发完成，验收通过 |
| `🚫 已阻塞` | 因依赖或风险无法推进 |
| `⏭ 已跳过` | 本版本决定跳过 |

### 1.2 整体进度统计

| 指标 | 当前值 | 目标值 |
|------|--------|--------|
| 总任务数 | 24 | 24 |
| 已完成 | 22 | 24 |
| 进行中 | 1 | — |
| 已阻塞 | 0 | 0 |
| 完成率 | 91.67% | 100% |
| 预估总工时 | 16.25 天 | — |
| 已消耗工时 | 15.5 天 | — |
| 剩余工时 | 0.75 天 | — |

### 1.3 批次进度概览

| 批次 | 任务数 | 已完成 | 进行中 | 待开始 | 阻塞 | 批次完成率 |
|------|--------|--------|--------|--------|------|-----------|
| 第 1 批：项目基础设施 | 4 | 4 | 0 | 0 | 0 | 100% |
| 第 2 批：核心后端功能 | 3 | 3 | 0 | 0 | 0 | 100% |
| 第 3 批：交互功能 | 5 | 5 | 0 | 0 | 0 | 100% |
| 第 4 批：主面板 UI | 8 | 8 | 0 | 0 | 0 | 100% |
| 第 5 批：集成联调测试 | 4 | 2 | 1 | 1 | 0 | 50% |
| **合计** | **24** | **22** | **1** | **1** | **0** | **91.67%** |

---

## 2. 主任务追踪表

> **状态更新规则**：变更状态时，同步填写「实际开始」「实际完成」「实际工时」列，并在第 6 节进度日志中追加记录。

| 任务 ID | 批次 | 任务名称 | 负责模块 | 依赖 | 预估工时 | 状态 | 实际开始 | 实际完成 | 实际工时 | 备注 |
|---------|------|----------|----------|------|---------|------|---------|---------|---------|------|
| TASK-01-1 | 第 1 批 | 初始化 Tauri + React 项目 | 基础设施 | — | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | |
| TASK-01-2 | 第 1 批 | 配置 Tauri 窗口参数 | M03 窗口管理 | — | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | |
| TASK-01-3 | 第 1 批 | 定义类型系统和错误类型 | M13/M09 | — | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | |
| TASK-01-4 | 第 1 批 | 搭建 CI 流水线（GitHub Actions） | 基础设施 | — | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | 本地已配置 workflow，待远端触发验证 |
| TASK-02-1 | 第 2 批 | 实现 M09 配置管理（基础版） | M09 配置管理 | TASK-01-3 | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | |
| TASK-02-2 | 第 2 批 | 实现 M01 内存存储（InMemoryStore） | M01 粘贴板监听 | TASK-01-3 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 8 个 Store + 5 个 History 单元测试通过 |
| TASK-02-3 | 第 2 批 | 实现 M14 平台抽象（macOS） | M14 平台抽象 | TASK-01-1 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 基于 arboard/objc/core-graphics 完成 macOS 实现 |
| TASK-03-1 | 第 3 批 | 实现 M01 粘贴板监听服务 | M01 粘贴板监听 | TASK-02-2, TASK-02-3 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 已完成 200ms 轮询与变更计数检测 |
| TASK-03-2 | 第 3 批 | 实现 M03 窗口管理 | M03 窗口管理 | TASK-01-2 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 已实现屏幕底部定位、show/hide/toggle |
| TASK-03-3 | 第 3 批 | 实现 M04 快捷键管理 | M04 快捷键管理 | TASK-03-2 | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | 已接入 tauri-plugin-global-shortcut |
| TASK-03-4 | 第 3 批 | 实现 M06 粘贴执行 | M06 粘贴执行 | TASK-03-1, TASK-03-2 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 粘贴流程与 UT-PASTE-001/002 通过 |
| TASK-03-5 | 第 3 批 | 实现 M13 IPC Command 和 Event | M13 IPC 通信 | TASK-02-2, TASK-03-4 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 5 个 Command + 2 个 Event 已接入 |
| TASK-04-1 | 第 4 批 | 实现 M12 前端状态管理 | M12 状态管理 | TASK-01-3 | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | Store 单测已覆盖边界场景 |
| TASK-04-2 | 第 4 批 | 实现 IPC 封装层 | M13 IPC 通信（前端） | TASK-01-3 | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | commands/events/errorHandler 已完成并有 Mock 测试 |
| TASK-04-3 | 第 4 批 | 实现 useClipboardEvents Hook | M12 状态管理 | TASK-04-1, TASK-04-2 | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | 事件订阅与卸载逻辑测试通过 |
| TASK-04-4 | 第 4 批 | 实现 useKeyboard Hook | M05 主面板 UI | TASK-04-1, TASK-04-2 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 方向键/Enter/Delete/Backspace/Escape 测试通过 |
| TASK-04-5 | 第 4 批 | 实现文本卡片组件（TextCard） | M05 主面板 UI | TASK-04-1 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | 短文本/长文本/选中样式测试通过 |
| TASK-04-6 | 第 4 批 | 实现空状态视图（EmptyState） | M05 主面板 UI | TASK-04-1 | 0.25 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.25 天 | 空状态组件与测试通过 |
| TASK-04-7 | 第 4 批 | 实现卡片列表和主面板容器 | M05 主面板 UI | TASK-04-5, TASK-04-6, TASK-04-4 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | MainPanel 6 个交互测试通过 |
| TASK-04-8 | 第 4 批 | 实现 Toast 提示组件 | M05 主面板 UI | — | 0.25 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.25 天 | Toast 组件与自动消失逻辑完成 |
| TASK-05-1 | 第 5 批 | 前后端集成联调 | 全局 | 第 4 批全部 | 1 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 1 天 | `pnpm tauri dev` 可启动，前后端链路联通 |
| TASK-05-2 | 第 5 批 | 全量单元测试修复 | 全局 | TASK-05-1 | 0.5 天 | ✅ 已完成 | 2026-03-06 | 2026-03-06 | 0.5 天 | `cargo test` / `pnpm test:coverage` 全绿 |
| TASK-05-3 | 第 5 批 | 手动验收测试 | 全局 | TASK-05-2 | 0.5 天 | 🔄 进行中 | 2026-03-06 | | | 待完成 30 分钟稳定性和全清单人工走查 |
| TASK-05-4 | 第 5 批 | 性能基准测试 | 全局 | TASK-05-3 | 0.25 天 | ⬜ 待开始 | | | | |

---

## 3. 各批次任务详情

### 3.1 第 1 批：项目基础设施

> **目标**：搭建可运行的项目骨架，建立开发环境和规范基础。第 1 批内所有任务可并行。

#### TASK-01-1：初始化 Tauri + React 项目

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 1 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | 基础设施 |
| 依赖任务 | — |
| 当前状态 | ✅ 已完成 |

**具体步骤**：

| # | 步骤 | 完成 |
|---|------|------|
| 1 | 执行 `pnpm create tauri-app` 创建项目（选择 React + TypeScript） | ☑ |
| 2 | 配置 `tailwind.config.ts`（声明颜色/间距 Token） | ☑ |
| 3 | 安装 Framer Motion：`pnpm add framer-motion` | ☑ |
| 4 | 安装 Zustand：`pnpm add zustand` | ☑ |
| 5 | 安装 Lucide React：`pnpm add lucide-react` | ☑ |
| 6 | 配置 ESLint + Prettier | ☑ |
| 7 | 配置 Vitest，安装 `@testing-library/react`、`@testing-library/user-event`、`jsdom` | ☑ |
| 8 | 创建 `src/__mocks__/@tauri-apps/api/core.ts` Mock 文件 | ☑ |
| 9 | 验证 `pnpm tauri dev` 可正常启动 | ☑ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | `pnpm tauri dev` 启动后，macOS 上弹出 Tauri 窗口 | ☑ |
| AC-2 | `pnpm test` 运行无报错（即使没有测试文件） | ☑ |
| AC-3 | `pnpm run lint` 无错误 | ☑ |
| AC-4 | Tailwind CSS 类名在浏览器中生效 | ☑ |

---

#### TASK-01-2：配置 Tauri 窗口参数

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 1 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M03 窗口管理 |
| 依赖任务 | — |
| 当前状态 | ✅ 已完成 |

**配置目标文件**：`tauri.conf.json`

**关键配置项**：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `decorations` | `false` | 无标题栏 |
| `transparent` | `true` | 背景透明 |
| `alwaysOnTop` | `true` | 始终置顶 |
| `skipTaskbar` | `true` | 不显示在 Dock/任务栏 |
| `visible` | `false` | 初始不可见 |
| `width` | `800` | 初始宽度（运行时动态设置） |
| `height` | `220` | 固定高度 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 窗口无标题栏 | ☑ |
| AC-2 | 窗口背景透明（不显示白色矩形） | ☑ |
| AC-3 | 窗口始终在其他应用上层 | ☑ |
| AC-4 | 窗口不出现在 macOS Dock 和任务栏 | ☑ |

---

#### TASK-01-3：定义类型系统和错误类型

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 1 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M13 IPC 通信、M09 配置 |
| 依赖任务 | — |
| 当前状态 | ✅ 已完成 |

**需创建文件**：

| 文件路径 | 职责 |
|----------|------|
| `src/types/clipboard.ts` | `ClipboardRecord`、`PasteMode`、`ContentType` |
| `src/types/error.ts` | `IpcError`、`ErrorCode` |
| `src/api/types.ts` | 从 types 重导出 + `NewRecordPayload`、`RecordDeletedPayload` |
| `src-tauri/src/error.rs` | `AppError` 枚举（6 个变体），实现 `serde::Serialize` |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 前端类型文件编译无错误（`pnpm run type-check`） | ☑ |
| AC-2 | 后端 `cargo check` 无错误 | ☑ |
| AC-3 | `AppError` 可被 `#[tauri::command]` 函数作为 `Err` 返回并序列化 | ☑ |

---

#### TASK-01-4：搭建 CI 流水线（GitHub Actions）

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 1 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | 基础设施 |
| 依赖任务 | — |
| 当前状态 | ✅ 已完成 |

**流水线阶段**：

| 阶段名 | 命令 | 触发条件 |
|--------|------|---------|
| `lint-frontend` | ESLint + TypeScript 类型检查 | 所有 PR/push |
| `lint-backend` | `cargo clippy` + `cargo fmt --check` | 所有 PR/push |
| `test-frontend` | Vitest 覆盖率测试 | 所有 PR/push |
| `test-backend` | `cargo test` | 所有 PR/push |
| `build` | `pnpm tauri build` | 仅 main 分支 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | Push 到 PR 后 CI 自动触发 | ☑ |
| AC-2 | 所有阶段在 macOS runner 上通过 | ☑ |

---

### 3.2 第 2 批：核心后端功能

> **目标**：实现后端数据模型、内存存储、平台适配层。第 2 批内所有任务可并行。

#### TASK-02-1：实现 M09 配置管理（基础版）

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 2 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M09 配置管理 |
| 依赖任务 | TASK-01-3 |
| 当前状态 | ✅ 已完成 |

**实现要点**：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `max_text_records` | `usize` | `20` | 最大记录数 |
| `toggle_shortcut` | `String` | `"Shift+Command+V"` | 调出面板快捷键 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | `AppConfig::default()` 返回预期默认值 | ☐ |
| AC-2 | `cargo test` 通过 | ☐ |

---

#### TASK-02-2：实现 M01 内存存储（InMemoryStore）

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 2 批 |
| 预估工时 | 1 天 |
| 负责模块 | M01 粘贴板监听 |
| 依赖任务 | TASK-01-3 |
| 当前状态 | ✅ 已完成 |

**需创建文件**：

| 文件路径 | 职责 |
|----------|------|
| `src-tauri/src/clipboard/types.rs` | `RecordId`、`ContentType`、`PasteMode` |
| `src-tauri/src/clipboard/record.rs` | `ClipboardRecord` 实体 |
| `src-tauri/src/clipboard/events.rs` | `ClipboardDomainEvent` |
| `src-tauri/src/clipboard/store.rs` | `InMemoryStore` |
| `src-tauri/src/clipboard/history.rs` | `ClipboardHistory` 聚合根 |
| `src-tauri/src/clipboard/repository.rs` | `ClipboardRecordRepository` trait + 内存实现 |
| `src-tauri/src/clipboard/test_fixtures.rs` | 测试夹具 |

**单元测试覆盖**：

| 测试 ID | 测试场景 | 覆盖 |
|---------|---------|------|
| UT-STORE-001 | 插入记录后可查询 | ☐ |
| UT-STORE-002 | 重复内容不插入 | ☐ |
| UT-STORE-003 | 超过上限自动淘汰最旧 | ☐ |
| UT-STORE-004 | 删除存在的记录 | ☐ |
| UT-STORE-005 | 删除不存在的记录返回错误 | ☐ |
| UT-STORE-006 | 按时间倒序返回记录 | ☐ |
| UT-STORE-007 | get_by_id 返回正确记录 | ☐ |
| UT-STORE-008 | 空存储返回空列表 | ☐ |
| UT-HIST-001 | add_record 触发 NewRecord 事件 | ☐ |
| UT-HIST-002 | 满载时触发 RecordEvicted 事件 | ☐ |
| UT-HIST-003 | 重复内容返回空事件列表 | ☐ |
| UT-HIST-004 | delete 触发 RecordDeleted 事件 | ☐ |
| UT-HIST-005 | 删除后列表长度减一 | ☐ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 8 个 InMemoryStore 测试全部通过 | ☐ |
| AC-2 | 5 个 ClipboardHistory 测试全部通过 | ☐ |
| AC-3 | `cargo test` 覆盖率 ≥ 80%（clipboard 模块） | ☐ |

---

#### TASK-02-3：实现 M14 平台抽象（macOS）

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 2 批 |
| 预估工时 | 1 天 |
| 负责模块 | M14 平台抽象 |
| 依赖任务 | TASK-01-1 |
| 当前状态 | ✅ 已完成 |

**Cargo.toml 依赖**：

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| `arboard` | `3` | 跨平台粘贴板读写 |
| `objc` | `0.2` | ObjC 桥接（macOS） |
| `core-graphics` | `0.23` | CGEvent 按键模拟（macOS） |

**接口实现要点**：

| 接口方法 | 实现方式 |
|----------|---------|
| `PlatformClipboard::read_text()` | 调用 `arboard::Clipboard::get_text()` |
| `PlatformClipboard::write_text()` | 调用 `arboard::Clipboard::set_text()` |
| `PlatformClipboard::change_count()` | ObjC 桥接读取 `NSPasteboard.changeCount` |
| `PlatformKeySimulator::simulate_paste()` | `CGEventCreateKeyboardEvent` 模拟 Cmd+V |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | `cargo build` 成功（macOS） | ☐ |
| AC-2 | 手动：`read_text()` 能返回当前粘贴板文本 | ☐ |
| AC-3 | 手动：`write_text("test")` 后系统粘贴板内容变为 "test" | ☐ |
| AC-4 | 手动：`simulate_paste()` 后文本粘贴到活动应用 | ☐ |

---

### 3.3 第 3 批：交互功能（部分并行）

> **目标**：实现粘贴板监听循环、快捷键注册、窗口管理和粘贴执行。

#### TASK-03-1：实现 M01 粘贴板监听服务

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 3 批 |
| 预估工时 | 1 天 |
| 负责模块 | M01 粘贴板监听 |
| 依赖任务 | TASK-02-2、TASK-02-3 |
| 当前状态 | ✅ 已完成 |

**核心参数**：

| 参数 | 值 | 说明 |
|------|-----|------|
| 轮询间隔 | 200ms | `tokio::time::sleep` |
| 变化检测 | `NSPasteboard.changeCount` 对比 | 避免重复读取 |
| 事件推送 | `emit_domain_event()` | 通知前端 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 在 macOS 上复制文本后，后端控制台输出新记录日志 | ☐ |
| AC-2 | 连续复制 5 条不同内容，store 中有 5 条记录 | ☐ |
| AC-3 | 重复复制相同内容，store 记录数不增加 | ☐ |
| AC-4 | 复制第 21 条内容，store 仍然只有 20 条 | ☐ |

---

#### TASK-03-2：实现 M03 窗口管理

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 3 批 |
| 预估工时 | 1 天 |
| 负责模块 | M03 窗口管理 |
| 依赖任务 | TASK-01-2 |
| 当前状态 | ✅ 已完成 |

**位置计算逻辑**：

| 参数 | 来源 | 说明 |
|------|------|------|
| `screen_width` | `app.primary_monitor()` | 屏幕宽度（动态获取） |
| `screen_height` | `app.primary_monitor()` | 屏幕高度 |
| `x` | `0` | 从屏幕左边缘开始 |
| `y` | `screen_height - panel_height` | 紧贴屏幕底部 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | `show()` 后，窗口出现在 macOS 屏幕底部 | ☐ |
| AC-2 | 窗口宽度等于屏幕宽度 | ☐ |
| AC-3 | `hide()` 后，窗口不可见 | ☐ |
| AC-4 | `toggle()` 交替显示/隐藏 | ☐ |

---

#### TASK-03-3：实现 M04 快捷键管理

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 3 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M04 快捷键管理 |
| 依赖任务 | TASK-03-2 |
| 当前状态 | ✅ 已完成 |

**快捷键配置**：

| 快捷键 | 触发动作 | 依赖插件 |
|--------|---------|---------|
| `Shift+Command+V` | `WindowManager::toggle()` | `tauri-plugin-global-shortcut = "2"` |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 在任意 macOS 应用中按 Shift+Command+V，面板出现 | ☐ |
| AC-2 | 再次按快捷键，面板消失 | ☐ |
| AC-3 | 快捷键响应时间 < 100ms（秒表手动测试） | ☐ |

---

#### TASK-03-4：实现 M06 粘贴执行

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 3 批 |
| 预估工时 | 1 天 |
| 负责模块 | M06 粘贴执行 |
| 依赖任务 | TASK-03-1、TASK-03-2 |
| 当前状态 | ✅ 已完成 |

**粘贴执行流程（严格顺序）**：

| 步骤 | 操作 | 目的 |
|------|------|------|
| 1 | `monitor.pause()` | 暂停监听，防止自我触发 |
| 2 | `repository.get_by_id(id)` | 获取记录内容 |
| 3 | `platform_clipboard.write_text()` | 写入粘贴板 |
| 4 | `window_manager.hide()` | 隐藏面板，让目标应用获得焦点 |
| 5 | `sleep(80ms)` | 等待窗口切换完成（关键！） |
| 6 | `platform_key_sim.simulate_paste()` | 模拟 Cmd+V |
| 7 | `monitor.resume()` | 恢复监听 |

**单元测试**：

| 测试 ID | 测试场景 | 覆盖 |
|---------|---------|------|
| UT-PASTE-001 | 正常粘贴流程 7 步骤按序执行 | ☐ |
| UT-PASTE-002 | 记录不存在时返回 RECORD_NOT_FOUND | ☐ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 在文本编辑器（TextEdit）中，面板选择记录按 Enter，内容出现在编辑器中 | ☐ |
| AC-2 | 粘贴后面板自动关闭 | ☐ |
| AC-3 | 粘贴操作不产生新的历史记录（监听暂停期间不记录） | ☐ |

---

#### TASK-03-5：实现 M13 IPC Command 和 Event

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 3 批 |
| 预估工时 | 1 天 |
| 负责模块 | M13 IPC 通信 |
| 依赖任务 | TASK-02-2、TASK-03-4 |
| 当前状态 | ✅ 已完成 |

**Command 清单**：

| Command 名 | 入参 | 返回值 | 说明 |
|------------|------|--------|------|
| `get_records` | `limit: usize` | `Vec<ClipboardRecord>` | 获取记录列表 |
| `delete_record` | `id: u64` | `()` | 删除指定记录 |
| `paste_record` | `id: u64, mode: PasteMode` | `()` | 粘贴指定记录 |
| `hide_panel` | — | `()` | 隐藏主面板 |
| `get_monitoring_status` | — | `bool` | 获取监听状态 |

**Event 清单**：

| Event 名 | Payload | 触发时机 |
|---------|---------|---------|
| `clipboard:new-record` | `{ record, evicted_id? }` | 新记录写入时 |
| `clipboard:record-deleted` | `{ id }` | 记录被删除时 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 前端调用 `invoke('get_records', {limit: 20})` 返回记录列表 | ☐ |
| AC-2 | 复制新内容后，前端收到 `clipboard:new-record` 事件 | ☐ |
| AC-3 | 前端调用 `invoke('delete_record', {id: 1})` 后，前端收到 `clipboard:record-deleted` 事件 | ☐ |

---

### 3.4 第 4 批：主面板 UI（并行）

> **目标**：实现前端 UI 组件、状态管理和事件订阅，完成可交互的界面。

#### TASK-04-1：实现 M12 前端状态管理

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M12 前端状态管理 |
| 依赖任务 | TASK-01-3 |
| 当前状态 | ✅ 已完成 |

**Store 设计**：

| Store | 状态字段 | 说明 |
|-------|---------|------|
| `useClipboardStore` | `records: ClipboardRecord[]` | 记录列表 |
| `useClipboardStore` | `selectedIndex: number` | 当前选中索引 |
| `useClipboardStore` | `isLoading: boolean` | 加载状态 |
| `useUIStore` | `isPanelVisible: boolean` | 面板可见性 |

**单元测试**：

| 测试 ID | 测试场景 | 覆盖 |
|---------|---------|------|
| UT-STORE-FE-001 | 添加记录后列表更新 | ☐ |
| UT-STORE-FE-002 | 删除记录后列表移除 | ☐ |
| UT-STORE-FE-003 | selectedIndex 边界不越界 | ☐ |
| UT-STORE-FE-004 | 超过 20 条时移除最旧记录 | ☐ |
| UT-UI-001 | showPanel 设置 isPanelVisible = true | ☐ |
| UT-UI-002 | hidePanel 设置 isPanelVisible = false | ☐ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 所有 Store 单元测试通过 | ☐ |
| AC-2 | `pnpm test:coverage` 报告显示 stores 覆盖率 ≥ 75% | ☐ |

---

#### TASK-04-2：实现 IPC 封装层

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M13 IPC 通信（前端） |
| 依赖任务 | TASK-01-3 |
| 当前状态 | ✅ 已完成 |

**需创建文件**：

| 文件路径 | 职责 |
|----------|------|
| `src/api/commands.ts` | 5 个 `invoke()` 封装函数 |
| `src/api/events.ts` | 2 个 `listen()` 封装 |
| `src/api/errorHandler.ts` | `parseIpcError()`、`getErrorMessage()` |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | TypeScript 编译无类型错误 | ☐ |
| AC-2 | Mock 环境下，调用 `getRecords()` 触发 `invoke('get_records', {limit: 20})` | ☐ |

---

#### TASK-04-3：实现 useClipboardEvents Hook

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | M12 前端状态管理 |
| 依赖任务 | TASK-04-1、TASK-04-2 |
| 当前状态 | ✅ 已完成 |

**事件处理逻辑**：

| 事件 | 处理动作 |
|------|---------|
| `clipboard:new-record` | `store.addRecord(record)`；若有 `evicted_id` 则 `store.removeRecord(evicted_id)` |
| `clipboard:record-deleted` | `store.removeRecord(id)` |
| 组件卸载 | 取消所有 listen 订阅 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 单元测试通过 | ☐ |
| AC-2 | 集成后：后端新记录到达时，前端列表实时更新 | ☐ |

---

#### TASK-04-4：实现 useKeyboard Hook

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 1 天 |
| 负责模块 | M05 主面板 UI |
| 依赖任务 | TASK-04-1、TASK-04-2 |
| 当前状态 | ✅ 已完成 |

**键盘事件映射**：

| 按键 | 动作 | 边界处理 |
|------|------|---------|
| `ArrowLeft` | `selectPrev()` | selectedIndex = 0 时无操作 |
| `ArrowRight` | `selectNext()` | selectedIndex = 末尾时无操作 |
| `Enter` | `pasteSelected('original')` | 无选中时无操作 |
| `Delete` | `deleteSelected()` | 列表为空时无操作 |
| `Backspace` | `deleteSelected()` | 同 Delete |
| `Escape` | `hidePanel()` | 始终有效 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 单元测试全部通过 | ☐ |
| AC-2 | 覆盖率 ≥ 75% | ☐ |

---

#### TASK-04-5：实现文本卡片组件（TextCard）

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 1 天 |
| 负责模块 | M05 主面板 UI |
| 依赖任务 | TASK-04-1 |
| 当前状态 | ✅ 已完成 |

**卡片区域规格**：

| 区域 | 高度 | 内容 | 样式要点 |
|------|------|------|---------|
| 顶部色块 | 28px | 「文本」标签 | 蓝色 `#007AFF`，白色文字 13px/600 |
| 时间戳行 | 20px | 相对时间 | 灰色 `#8E8E93`，12px |
| 内容区 | 弹性 | 文本预览 | 3 行截断，14px，行高 20px |
| 底部元信息 | 28px | 字符数 + 序号 | 两端对齐，11px |

**时间戳格式**：

| 时差范围 | 显示格式 | 示例 |
|---------|---------|------|
| < 60 秒 | 刚刚 | 刚刚 |
| 1 分钟 ~ 60 分钟 | N 分钟前 | 5 分钟前 |
| 1 小时 ~ 24 小时 | N 小时前 | 3 小时前 |
| 24 ~ 48 小时 | 昨天 | 昨天 |
| > 48 小时 | N 天前 | 2 天前 |

**单元测试**：

| 测试 ID | 测试场景 | 覆盖 |
|---------|---------|------|
| UT-CARD-001 | 短文本正常渲染 | ☐ |
| UT-CARD-002 | 长文本触发截断（3 行省略） | ☐ |
| UT-CARD-003 | 选中状态视觉正确（高亮边框） | ☐ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 单元测试全部通过 | ☐ |
| AC-2 | 视觉上符合《UX 设计文档》卡片视觉规格 | ☐ |
| AC-3 | 选中状态视觉清晰可辨 | ☐ |

---

#### TASK-04-6：实现空状态视图（EmptyState）

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 0.25 天 |
| 负责模块 | M05 主面板 UI |
| 依赖任务 | TASK-04-1 |
| 当前状态 | ✅ 已完成 |

**内容规格**：

| 元素 | 内容 | 样式 |
|------|------|------|
| 图标 | `<Clipboard>` (Lucide) | 40×40px，`#C7C7CC` |
| 主标题 | 还没有复制记录 | 15px，500，Text Secondary |
| 副标题 | 复制任何内容后将自动出现在这里 | 13px，400，Text Disabled |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 历史记录为空时，显示空状态图标 + 提示文案 | ☐ |
| AC-2 | 布局居中，视觉平衡 | ☐ |

---

#### TASK-04-7：实现卡片列表和主面板容器

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 1 天 |
| 负责模块 | M05 主面板 UI |
| 依赖任务 | TASK-04-5、TASK-04-6、TASK-04-4 |
| 当前状态 | ✅ 已完成 |

**组件职责**：

| 组件文件 | 职责 |
|----------|------|
| `CardList.tsx` | 横向 flex 布局，渲染 TextCard 列表，隐藏滚动条 |
| `MainPanel/index.tsx` | Framer Motion AnimatePresence 面板容器，管理弹出/收起动画 |

**动画参数**：

| 动画阶段 | `y` | `opacity` | 时长 | 缓动 |
|---------|-----|-----------|------|------|
| 入场（hidden→visible） | 100%→0 | 0→1 | 200ms | `ease-out` |
| 退场（visible→exit） | 0→100% | 1→0 | 180ms | `ease-in` |

**单元测试**：

| 测试 ID | 测试场景 | 覆盖 |
|---------|---------|------|
| UT-PANEL-001 | 有记录时渲染卡片列表 | ☐ |
| UT-PANEL-002 | 无记录时渲染空状态 | ☐ |
| UT-PANEL-003 | ArrowRight 切换到下一张卡片 | ☐ |
| UT-PANEL-004 | ArrowLeft 切换到上一张卡片 | ☐ |
| UT-PANEL-005 | Enter 触发 paste_record | ☐ |
| UT-PANEL-006 | Delete 触发 delete_record | ☐ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 面板 UI 测试（键盘操作）全部通过 | ☐ |
| AC-2 | 卡片横向排列，选中卡片高亮 | ☐ |
| AC-3 | 面板弹出/收起动画流畅（200ms） | ☐ |

---

#### TASK-04-8：实现 Toast 提示组件

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 4 批 |
| 预估工时 | 0.25 天 |
| 负责模块 | M05 主面板 UI（公共组件） |
| 依赖任务 | — |
| 当前状态 | ✅ 已完成 |

**Toast 规格**：

| 属性 | 值 |
|------|-----|
| 位置 | 主面板顶部，水平居中 |
| 背景色 | `rgba(0,0,0,0.75)`（不受主题影响） |
| 文字颜色 | `#FFFFFF`，14px |
| 圆角 | 8px |
| 自动消失 | 3 秒 |
| 进场动画 | 淡入 200ms |
| 退场动画 | 淡出 200ms |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 调用 `showToast("粘贴失败，请重试")` 后，屏幕顶部出现提示 | ☐ |
| AC-2 | 3 秒后自动消失 | ☐ |
| AC-3 | 深色背景，不受主题影响 | ☐ |

---

### 3.5 第 5 批：集成联调 + 测试 + 验收（串行）

> **目标**：将前后端连通，完成功能集成测试，确保核心流程无阻断运行。

#### TASK-05-1：前后端集成联调

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 5 批 |
| 预估工时 | 1 天 |
| 负责模块 | 全局 |
| 依赖任务 | 第 4 批全部完成 |
| 当前状态 | ✅ 已完成 |

**核心联调流程**：

| # | 联调场景 | 涉及模块链 | 通过 |
|---|---------|-----------|------|
| 1 | 复制文本 → 面板出现记录卡片 | M01 监听 → M13 事件 → M12 Store → M05 UI | ☐ |
| 2 | 按快捷键 → 面板弹出 | M04 快捷键 → M03 窗口 → M05 动画 | ☐ |
| 3 | 方向键切换 → 选中变化 | M05 键盘 → M12 Store → M05 UI | ☐ |
| 4 | Enter → 粘贴 + 面板关闭 | M05 → M13 IPC → M06 PasteService → M14 按键模拟 | ☐ |
| 5 | Delete → 记录消失 + 前端同步 | M05 → M13 IPC → M01 Store → M13 事件 → M12 → M05 | ☐ |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 完整流程无阻断执行 | ☐ |
| AC-2 | 控制台无 Error 级别日志 | ☐ |

---

#### TASK-05-2：全量单元测试修复

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 5 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | 全局 |
| 依赖任务 | TASK-05-1 |
| 当前状态 | ✅ 已完成 |

**覆盖率目标**：

| 范围 | 命令 | 目标覆盖率 |
|------|------|-----------|
| 后端（Rust） | `cargo test` | ≥ 80% |
| 前端（TypeScript） | `pnpm test:coverage` | ≥ 75% |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 后端 `cargo test` 全绿，覆盖率 ≥ 80% | ☐ |
| AC-2 | 前端 `pnpm test:coverage` 全绿，覆盖率 ≥ 75% | ☐ |

---

#### TASK-05-3：手动验收测试

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 5 批 |
| 预估工时 | 0.5 天 |
| 负责模块 | 全局 |
| 依赖任务 | TASK-05-2 |
| 当前状态 | 🔄 进行中 |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 《BDD 自动化验收文档》§10 手动验收清单全部打勾 | ☐ |
| AC-2 | 快捷键响应时间 < 100ms（实测） | ☐ |
| AC-3 | 内存占用 < 80MB（Activity Monitor 查看） | ☐ |
| AC-4 | 连续使用 30 分钟无崩溃 | ☐ |

---

#### TASK-05-4：性能基准测试

| 属性 | 内容 |
|------|------|
| 所属批次 | 第 5 批 |
| 预估工时 | 0.25 天 |
| 负责模块 | 全局 |
| 依赖任务 | TASK-05-3 |
| 当前状态 | ⬜ 待开始 |

**性能指标**：

| 指标 | 测量工具 | 目标 | 实测值 |
|------|---------|------|--------|
| 快捷键响应时间 | 秒表 / Instruments | < 100ms | |
| 空闲内存占用 | Activity Monitor | < 80MB | |
| CPU 占用（后台） | Activity Monitor | < 1% | |

**验收标准**：

| # | 验收项 | 通过 |
|---|--------|------|
| AC-1 | 3 项指标均达标 | ☐ |

---

## 4. 任务依赖关系图

```
第 1 批（并行）
├── TASK-01-1  ──────────────────────────────────────► TASK-02-3
├── TASK-01-2  ──────────────────────────────────────► TASK-03-2 ─► TASK-03-3
├── TASK-01-3  ──┬──────────────────────────────────► TASK-02-1
│               ├──────────────────────────────────► TASK-02-2 ─► TASK-03-1 ─► TASK-03-4 ─► TASK-03-5
│               ├──────────────────────────────────► TASK-04-1 ─┬─► TASK-04-3 ─► ...
│               └──────────────────────────────────► TASK-04-2 ─┘
└── TASK-01-4  （独立，无下游依赖）

第 3 批串行关系
TASK-03-1 ──► TASK-03-4 ──► TASK-03-5
TASK-03-2 ──► TASK-03-3

第 4 批内部依赖
TASK-04-1 + TASK-04-2 ──► TASK-04-3
TASK-04-1 + TASK-04-2 ──► TASK-04-4
TASK-04-5 + TASK-04-6 + TASK-04-4 ──► TASK-04-7

第 5 批（严格串行）
第 4 批全完成 ──► TASK-05-1 ──► TASK-05-2 ──► TASK-05-3 ──► TASK-05-4
```

---

## 5. 风险登记册

| 风险 ID | 风险描述 | 影响任务 | 影响等级 | 缓解措施 | 当前状态 |
|---------|---------|---------|---------|---------|---------|
| RISK-01 | macOS 辅助功能权限被拒导致按键模拟失效 | TASK-02-3, TASK-03-4 | 高 | 启动时检测权限，打印友好日志；手动授权后重启 | ⬜ 未触发 |
| RISK-02 | `arboard` 在 macOS 获取 `change_count` 需 ObjC 桥接，可能兼容性问题 | TASK-02-3 | 中 | 提前 POC 验证；若失败改用文本内容哈希比较 | ⬜ 未触发 |
| RISK-03 | Framer Motion 动画在 Tauri WebView 上性能异常 | TASK-04-7 | 中 | 回退到纯 CSS `@keyframes` 动画 | ⬜ 未触发 |
| RISK-04 | 面板失焦后无法关闭，用户卡在面板中 | TASK-03-2, TASK-04-7 | 中 | 监听 `blur` 事件，面板失焦时自动调用 `hide_panel` | ⬜ 未触发 |

---

## 6. 进度日志

> **记录规则**：每次变更任务状态、遇到阻塞或完成重要里程碑时，在此追加一条日志记录。

| 日期 | 任务 ID | 事件类型 | 状态变更 | 实际工时 | 记录者 | 备注 |
|------|---------|---------|---------|---------|--------|------|
| 2026-03-06 | TASK-01-1 | 完成 | `🔄 进行中` → `✅ 已完成` | 0.5 天 | Codex | 完成项目初始化、Tailwind/ESLint/Prettier/Vitest、Tauri mock 与本地命令验证 |
| 2026-03-06 | TASK-01-2 | 完成 | `🔄 进行中` → `✅ 已完成` | 0.5 天 | Codex | 完成无边框/透明/置顶/隐藏任务栏等窗口参数配置 |
| 2026-03-06 | TASK-01-3 | 完成 | `🔄 进行中` → `✅ 已完成` | 0.5 天 | Codex | 已创建前后端共享类型与 `AppError` 序列化错误模型 |
| 2026-03-06 | TASK-01-4 | 完成 | `🔄 进行中` → `✅ 已完成` | 0.5 天 | Codex | 已新增 `.github/workflows/ci.yml`，包含 lint/test/build 阶段 |
| 2026-03-06 | TASK-02-1 | 完成 | `🔄 进行中` → `✅ 已完成` | 0.5 天 | Codex | AppConfig 默认值与测试通过 |
| 2026-03-06 | TASK-02-2~TASK-03-5 | 里程碑 | 第 2 批 + 第 3 批任务全部完成 | 6.0 天 | Codex | 后端存储/平台抽象/监听/窗口/快捷键/粘贴/IPC 已联通，`cargo test` 与 `cargo clippy -D warnings` 通过 |
| 2026-03-06 | TASK-04-1~TASK-04-8 | 里程碑 | 第 4 批任务全部完成 | 5.0 天 | Codex | 前端 store/api/hook/UI 组件与交互测试完成，`pnpm test:coverage` 分支覆盖率 88.31% |
| 2026-03-06 | TASK-05-1 | 完成 | `⬜ 待开始` → `✅ 已完成` | 1 天 | Codex | 前后端集成启动验证通过（`pnpm tauri dev`） |
| 2026-03-06 | TASK-05-2 | 完成 | `⬜ 待开始` → `✅ 已完成` | 0.5 天 | Codex | 全量自动化测试通过：`cargo test` + `pnpm test:coverage` |
| 2026-03-06 | TASK-05-3 | 开始 | `⬜ 待开始` → `🔄 进行中` | — | Codex | 需补齐人工验收清单与 30 分钟稳定性观测 |
| 2026-03-06 | TASK-05-4 | 备注 | 性能预采样（release） | 0.1 天 | Codex | RSS ≈ 74MB、空闲 CPU ≈ 0%，快捷键响应待人工实测 |

**事件类型说明**：

| 类型 | 含义 |
|------|------|
| `开始` | 任务从「待开始」变为「进行中」 |
| `完成` | 任务从「进行中」变为「已完成」 |
| `阻塞` | 任务状态变为「已阻塞」，需记录阻塞原因 |
| `解除阻塞` | 任务从「已阻塞」重新变为「进行中」 |
| `里程碑` | 批次完成、MVP 完成等重要节点 |
| `风险触发` | 风险登记册中某风险实际发生 |
| `备注` | 其他重要信息记录 |

---

## 7. 时间线规划

| 批次 | 任务 | 预估工时 | 可并行 | 建议时段 |
|------|------|---------|--------|---------|
| 第 1 批 | TASK-01-1 ~ 01-4 | 2 天 | 是（全部并行） | Day 1–2 |
| 第 2 批 | TASK-02-1 ~ 02-3 | 2.5 天 | 是（全部并行） | Day 3–4 |
| 第 3 批 | TASK-03-1 ~ 03-5 | 4.5 天 | 部分并行 | Day 5–8 |
| 第 4 批 | TASK-04-1 ~ 04-8 | 4.5 天 | 是（大部分并行） | Day 6–10（与第 3 批后半段并行） |
| 第 5 批 | TASK-05-1 ~ 05-4 | 2.25 天 | 否（严格串行） | Day 11–13 |
| **合计** | **24 个任务** | **~16.25 天** | — | **约 12–14 工作日** |

---

**文档版本**：v1.1
**编写日期**：2026-03-05
**版本范围**：v0.1 MVP
**文档状态**：已完成
