# v0.3 版本迭代日志记录

> 更新时间：2026-03-07
> 分支：`main`
> 当前阶段：v0.3 托盘与运行控制推进中（6/16 任务完成）

---

## 1. 迭代总览

| 版本标记 | 日期 | 状态 | 完成度 | 说明 |
|---|---|---|---|---|
| v0.3.0-doc.1 | 2026-03-07 | 已完成 | 1/16 | 建立 v0.3 全套设计、契约、页面、UX 与冲刺文档，锁定当前版本范围与实施顺序 |
| v0.3.0-feat.1 | 2026-03-07 | 已完成 | 2/16 | 扩展 `config.json` 支持 `launch_at_login`，补齐配置兼容与持久化更新能力 |
| v0.3.0-feat.2 | 2026-03-07 | 已完成 | 3/16 | 新增 Launch Agent 服务并在启动阶段执行自启动弱修复 |
| v0.3.0-feat.3 | 2026-03-07 | 已完成 | 4/16 | 建立托盘状态枚举并补齐活动/暂停两套托盘图标资源 |
| v0.3.0-feat.4 | 2026-03-07 | 已完成 | 5/16 | 初始化系统托盘并接通显示 / 隐藏主面板、退出应用行为 |
| v0.3.0-feat.5 | 2026-03-07 | 已完成 | 6/16 | 新增监听状态切换命令和 `system:monitoring-changed` 事件广播 |

---

## 2. 本次变更明细

### 2.1 2026-03-07 `v0.3-doc-baseline`

**变更类型**：`docs(v0.3)`

**主要变更**：
- 新增 `docs/v0.3/用户故事文档.md`，定义系统托盘、开机自启动、监听控制、数字键快选、多显示器等当前版本需求边界
- 新增 `docs/v0.3/DDD战术设计文档.md`、`docs/v0.3/数据库设计文档.md`、`docs/v0.3/API契约文档.md`、`docs/v0.3/状态码契约文档.md`，锁定领域模型、存储策略与 IPC 协议
- 新增 `docs/v0.3/自动化测试文档.md`、`docs/v0.3/BDD自动化验收文档.md`、`docs/v0.3/种子数据契约文档.md`，定义测试分层、BDD 场景与夹具约束
- 新增 `docs/v0.3/文件目录结构设计文档.md`、`docs/v0.3/页面地图与每个页面的详细功能描述文档.md`、`docs/v0.3/UX设计文档.md`、`docs/v0.3/冲刺任务文档.md`，补齐实现视图与任务拆解
- 更新 `docs/项目决策记录文档.md`，固化 v0.3 版本范围与存储决策

**产出结果**：
- `v0.3` 当前版本范围已经锁定，不再把设置页、搜索页提前到当前迭代
- 后续开发可以直接按 `docs/v0.3/冲刺任务文档.md` 顺序推进，实现、测试与验收边界统一
- 数据库存储与系统集成存储职责被明确拆开，降低后续实现歧义

**验证结果**：
- 文档目录 `docs/v0.3/` 已创建 ✅
- 目标文档已全部落盘 ✅
- 迭代日志与项目决策记录已同步更新 ✅

### 2.2 2026-03-07 `v0.3-config-launch-at-login`

**变更类型**：`feat(config)`

**主要变更**：
- 更新 `src-tauri/src/config/schema.rs`，为 `AppConfig` 增加 `launch_at_login` 字段，默认值为 `true`
- 更新 `src-tauri/src/config/mod.rs`，新增 `ConfigStore`，统一管理配置快照读取与 `launch_at_login` 的写回
- 补齐旧配置升级写回测试和新字段持久化测试，保证历史配置文件兼容 `v0.3`

**产出结果**：
- `config.json` 现在可稳定表达 `launch_at_login` 偏好，并和 v0.3 文档契约保持一致
- 后续 `TASK-01-2` 的 Launch Agent 服务可以直接复用配置存储能力，无需再临时拼接文件写回逻辑

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml config::` ✅

### 2.3 2026-03-07 `v0.3-launch-agent-service`

**变更类型**：`feat(autostart)`

**主要变更**：
- 新增 `src-tauri/src/autostart/mod.rs`，实现 Launch Agent plist 的生成、查询、删除、幂等切换与弱修复
- 更新 `src-tauri/src/lib.rs`，在应用启动时注入 `LaunchAgentService`，并根据配置执行 `reconcile`
- 更新 `src-tauri/src/state.rs` 与 `src-tauri/src/error.rs`，为后续运行态、自启动切换和错误提示提供状态与错误分类基础

**产出结果**：
- `launch_at_login` 不再只是配置字段，而是已经有对应的 macOS Launch Agent 服务层承接
- 应用启动时可以自动修复“配置为开启但 plist 缺失”或“配置为关闭但 plist 残留”的常见不一致情况

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml autostart::` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml config::` ✅

### 2.4 2026-03-07 `v0.3-tray-assets`

**变更类型**：`feat(tray)`

**主要变更**：
- 新增 `src-tauri/src/tray/mod.rs`，整理托盘状态枚举、运行态快照、菜单项 ID 与图标资源加载入口
- 新增 `src-tauri/icons/tray-active.png` 与 `src-tauri/icons/tray-paused.png`，作为托盘正常态与暂停态资源
- 为托盘状态文案和资源嵌入增加单元测试，确保第 2 批菜单行为实现时直接可复用

**产出结果**：
- 托盘相关实现不再散落在后续命令或窗口模块中，而是拥有明确的独立领域入口
- 第 2 批任务只需要补菜单构建、点击路由和状态刷新，不必再重复设计状态模型或准备图标资源

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml tray::` ✅

### 2.5 2026-03-07 `v0.3-tray-toggle-and-exit`

**变更类型**：`feat(tray)`

**主要变更**：
- 将 `src-tauri/src/tray/mod.rs` 扩展为 `TrayController`，统一负责托盘初始化、菜单项句柄管理和状态刷新
- 在 `src-tauri/src/lib.rs` 的启动阶段接入系统托盘初始化，失败时仅记录日志，不阻塞主面板快捷键主链路
- 实现托盘菜单“显示主面板 / 隐藏主面板”“退出应用”“打开日志目录”的真实动作路由

**产出结果**：
- 应用已经拥有系统托盘入口，用户不再只能依赖全局快捷键唤起主面板
- 托盘初始化失败时应用仍可继续启动，符合 v0.3 的降级体验要求

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml tray::` ✅

### 2.6 2026-03-07 `v0.3-monitoring-toggle`

**变更类型**：`feat(monitoring)`

**主要变更**：
- 更新 `src-tauri/src/ipc/commands.rs`，新增 `set_monitoring` 命令并在恢复监听前执行剪贴板状态同步
- 更新 `src-tauri/src/ipc/events.rs` 与 `src-tauri/src/clipboard/monitor.rs`，新增 `system:monitoring-changed` 事件广播能力
- 更新前端 `src/api/commands.ts`、`src/api/events.ts`、错误码定义与对应测试，使监听控制链路从后端到前端契约打通

**产出结果**：
- 监听暂停 / 恢复不再只是内部状态切换，而是已经拥有统一的 IPC 与事件广播出口
- 恢复监听时会先同步当前系统剪贴板快照，避免暂停期间复制的敏感内容在恢复后被补录

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml monitor::` ✅
- `pnpm run test src/__tests__/api/commands.test.ts src/__tests__/api/events.test.ts` ✅

---

## 3. 当前版本结论

### 已锁定事项

- `v0.3` 聚焦系统托盘、开机自启动、暂停 / 恢复监听、数字键快选、多显示器与动效增强
- 设置页、自定义快捷键、搜索页继续留在 `v0.5+`
- SQLite 业务表结构继续沿用 `v0.2`，系统集成偏好落在 `config.json` 与 Launch Agent plist
- `launch_at_login` 已落入 `config.json` 并具备向旧配置自动回填的兼容能力
- Launch Agent 服务层已落地，后续可直接把托盘勾选、自启动状态查询接到现有实现上
- 托盘状态枚举与图标资源已落地，系统集成基建批次已经闭环
- 托盘初始化、显示 / 隐藏主面板和退出路由已打通，第 2 批开始进入运行控制能力实现
- 暂停 / 恢复监听的命令与事件广播已打通，运行控制主链路完成度进一步提升

### 下一步建议

- 继续推进 `TASK-02-3`，实现清空历史确认流程、资源清理与事件广播
- 然后进入多显示器定位和运行态状态同步任务
