# v0.3 冲刺任务文档

> 更新时间：2026-03-07
> 版本阶段：v0.3 Feature Complete
> 当前状态：动效与状态反馈推进中（12/16 任务完成）

---

## 1. 进度总览仪表盘

### 1.1 状态图例

| 标记 | 含义 |
|------|------|
| `✅ 已完成` | 已开发并通过自动化验证 |
| `🟡 进行中` | 已领取，正在开发 |
| `⬜ 未开始` | 尚未开始 |
| `⛔ 阻塞` | 受外部依赖或技术风险影响 |

### 1.2 整体进度统计

| 指标 | 数值 |
|------|------|
| 总任务数 | `16` |
| 已完成 | `12` |
| 进行中 | `0` |
| 未开始 | `4` |
| 完成度 | `75%` |

### 1.3 批次进度概览

| 批次 | 名称 | 进度 |
|------|------|------|
| 第 0 批 | 文档基线 | `1/1` |
| 第 1 批 | 系统集成基建 | `3/3` |
| 第 2 批 | 托盘与运行控制 | `3/3` |
| 第 3 批 | 主面板交互增强 | `3/3` |
| 第 4 批 | 动效与状态反馈 | `2/3` |
| 第 5 批 | 自动化测试与验收 | `0/3` |

---

## 2. 主任务追踪表

| 任务 ID | 任务名称 | 批次 | 优先级 | 状态 |
|---------|----------|------|--------|------|
| `TASK-00-1` | 建立 v0.3 全套文档基线 | 第 0 批 | P0 | `✅ 已完成` |
| `TASK-01-1` | 扩展 `config.json` 支持 `launch_at_login` | 第 1 批 | P0 | `✅ 已完成` |
| `TASK-01-2` | 实现 macOS Launch Agent 服务 | 第 1 批 | P0 | `✅ 已完成` |
| `TASK-01-3` | 设计托盘状态与图标资源 | 第 1 批 | P1 | `✅ 已完成` |
| `TASK-02-1` | 实现托盘显示 / 隐藏 / 退出 | 第 2 批 | P0 | `✅ 已完成` |
| `TASK-02-2` | 实现暂停 / 恢复监听与事件广播 | 第 2 批 | P0 | `✅ 已完成` |
| `TASK-02-3` | 实现清空历史与确认流程 | 第 2 批 | P0 | `✅ 已完成` |
| `TASK-03-1` | 实现数字键 `1-9` 快选 | 第 3 批 | P0 | `✅ 已完成` |
| `TASK-03-2` | 扩展窗口定位支持多显示器 | 第 3 批 | P0 | `✅ 已完成` |
| `TASK-03-3` | 补齐运行态状态查询与前端同步 | 第 3 批 | P1 | `✅ 已完成` |
| `TASK-04-1` | 完成主面板显隐动画打磨 | 第 4 批 | P1 | `✅ 已完成` |
| `TASK-04-2` | 完成卡片切换 / 删除动画与降级 | 第 4 批 | P1 | `✅ 已完成` |
| `TASK-04-3` | 完成监听暂停提示与错误反馈 | 第 4 批 | P1 | `⬜ 未开始` |
| `TASK-05-1` | 补齐 Rust 单元 / 集成测试 | 第 5 批 | P0 | `⬜ 未开始` |
| `TASK-05-2` | 补齐前端测试与系统状态测试 | 第 5 批 | P0 | `⬜ 未开始` |
| `TASK-05-3` | 建立 BDD / E2E 场景并完成回归 | 第 5 批 | P0 | `⬜ 未开始` |

---

## 3. 各批次任务详情

### 3.1 第 0 批：文档基线

| 任务 ID | 说明 | 交付物 | 状态 |
|---------|------|--------|------|
| `TASK-00-1` | 建立 v0.3 用户故事、契约、页面、UX、冲刺等全套文档 | `docs/v0.3/*.md` | `✅ 已完成` |

### 3.2 第 1 批：系统集成基建

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-01-1` | 配置模型新增 `launch_at_login` 并兼容旧配置 | 无 | `✅ 已完成` |
| `TASK-01-2` | 实现 Launch Agent 生成、查询、删除 | `TASK-01-1` | `✅ 已完成` |
| `TASK-01-3` | 准备托盘图标资源与托盘状态枚举 | 无 | `✅ 已完成` |

### 3.3 第 2 批：托盘与运行控制

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-02-1` | 托盘菜单实现显示 / 隐藏主面板与退出应用 | `TASK-01-3` | `✅ 已完成` |
| `TASK-02-2` | 实现暂停 / 恢复监听及 `system:monitoring-changed` | `TASK-02-1` | `✅ 已完成` |
| `TASK-02-3` | 实现 `clear_history`、确认流程与资源清理 | `TASK-02-1` | `✅ 已完成` |

### 3.4 第 3 批：主面板交互增强

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-03-1` | 扩展 `useKeyboard` 支持数字键 `1-9` 快选 | 无 | `✅ 已完成` |
| `TASK-03-2` | 使用活动显示器 `work_area` 计算主面板位置 | 无 | `✅ 已完成` |
| `TASK-03-3` | 新增 `SystemStore` 与运行态查询同步 | `TASK-02-2` | `✅ 已完成` |

### 3.5 第 4 批：动效与状态反馈

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-04-1` | 打磨主面板弹出 / 收起动画 | `TASK-03-1` | `✅ 已完成` |
| `TASK-04-2` | 打磨卡片切换 / 删除动画，并兼容 reduced motion | `TASK-03-1` | `✅ 已完成` |
| `TASK-04-3` | 增加监听暂停提示、清空历史成功 / 失败反馈 | `TASK-02-2`、`TASK-02-3` | `⬜ 未开始` |

### 3.6 第 5 批：自动化测试与验收

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-05-1` | Rust 单元 / 集成测试覆盖托盘、自启动、多显示器、清空历史 | 前 4 批核心功能完成 | `⬜ 未开始` |
| `TASK-05-2` | 前端测试覆盖数字键、系统状态同步、确认弹窗 | 前 4 批前端功能完成 | `⬜ 未开始` |
| `TASK-05-3` | BDD / E2E 验收场景与性能回归 | `TASK-05-1`、`TASK-05-2` | `⬜ 未开始` |

---

## 4. 风险与缓解

| 风险 | 说明 | 缓解方案 |
|------|------|----------|
| 托盘 API 平台差异 | Tauri 托盘在不同 macOS 版本表现可能不同 | 先锁定 macOS 单平台，增加 Mock 与真机回归 |
| Launch Agent 写入权限 | 用户环境可能限制写入或存在旧残留 | 统一走临时目录测试，真实环境错误写日志并回滚 UI |
| 多显示器定位复杂 | Dock 位置与工作区计算容易出错 | 独立 `window/position.rs` 并补齐布局夹具 |

---

## 5. 冲刺完成后建议

- 完成 `v0.3` 后再进入 `v0.5` 设置页，避免在无系统集成基线时直接堆配置 UI
- 若托盘与自启动落地稳定，可在 `v0.5` 复用当前系统状态接口给设置页

---

## 6. 最新进展

### 2026-03-07：完成 `TASK-04-2` 打磨卡片切换 / 删除动画，并兼容 reduced motion

**状态**：`✅ 已完成`

**完成内容**：
- 为卡片列表引入 `AnimatePresence` + `motion.div` 包裹层，补齐删除离场动画与列表重排过渡
- 卡片根节点新增边框 / 阴影过渡，键盘切换与数字键快选时的选中态变化更平滑
- 将 reduced motion 兼容下沉到 `motion.ts`，对主面板和卡片动效统一提供“弱动画 / 无位移缩放”降级策略
- 扩展动效测试，覆盖卡片删除离场配置和 reduced motion 配置分支

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-04-1` 打磨主面板弹出 / 收起动画

**状态**：`✅ 已完成`

**完成内容**：
- 将主面板显隐动效抽离为独立 `panelMotionVariants` 配置，统一进入 / 退出状态、缓动曲线和时长参数
- 打开动效对齐 UX 文档的 `220ms`，关闭动效对齐 `180ms`，并补充底部原点缩放，让面板更接近“就地弹出”
- 为主面板容器补充独立测试标识，方便后续第 4 批继续扩展卡片动效和 reduced motion 降级策略
- 新增 `panelMotion` 单元测试，锁定主面板显隐动画的关键时序约束

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-03-3` 补齐运行态状态查询与前端同步

**状态**：`✅ 已完成`

**完成内容**：
- 后端新增 `get_runtime_status` IPC，统一返回 `monitoring`、`launch_at_login`、`panel_visible` 三项运行态快照
- 前端新增 `SystemStore`，收敛监听状态、开机自启动、面板显隐和托盘可用性，并在 `App` 启动 / 聚焦时自动同步运行态
- `useSystemEvents` 现在会把 `system:monitoring-changed` 和清空历史相关动作同步写入系统状态与 UI 状态，减少前后端状态漂移
- 补齐 `commands`、`App`、`SystemStore` 与系统事件测试，覆盖运行态查询、状态水合与事件驱动更新链路

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-03-2` 扩展窗口定位支持多显示器

**状态**：`✅ 已完成`

**完成内容**：
- 将主面板定位逻辑从“固定主显示器”调整为“优先跟随鼠标所在显示器的 `work_area`，主显示器兜底”
- 在 `src-tauri/src/window/position.rs` 新增目标工作区选择逻辑，并保留 Dock 位于底部 / 侧边时的布局计算约束
- `src-tauri/src/window/mod.rs` 接入可用显示器与鼠标位置读取，保证快捷键与托盘打开面板时使用同一定位策略
- 补齐多显示器选择与兜底场景测试，覆盖鼠标在副屏、鼠标位置不可用、坐标越界等边界情况

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml window::` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-03-1` 实现数字键 `1-9` 快选

**状态**：`✅ 已完成`

**完成内容**：
- 扩展 `src/hooks/useKeyboard.ts`，支持主面板可见时通过数字键 `1-9` 直接选中前 9 条记录，并保持越界数字键无副作用
- 为主面板卡片新增数字槽位标记，且仅前 9 条显示；底部快捷键栏同步补充 `1-9 快选` 提示
- 额外收紧键盘事件边界：确认弹窗开启时不再触发主面板快捷键，避免危险操作与粘贴快捷键互相干扰
- 补齐 hook、主面板与卡片层测试，覆盖数字键快选、快选后 `Enter` 粘贴、UI 提示与槽位数量约束

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-02-3` 实现清空历史与确认流程

**状态**：`✅ 已完成`

**完成内容**：
- 后端新增 `clear_history` IPC、SQLite 批量清理能力与图片资源删除统计，并通过 `clipboard:history-cleared` 广播清理结果
- 托盘“清空历史”菜单改为先唤起主面板，并通过 `system:clear-history-requested` 触发前端确认弹窗，确认后再执行危险操作
- 前端新增 `ConfirmDialog`、`useSystemEvents` 和对应 API 封装，支持清空历史成功清空列表、关闭弹窗，失败保留现场并提示错误
- 补齐 Rust 与前端自动化测试，覆盖数据库清理、图片资源删除、事件订阅、确认弹窗与 App 集成链路

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-02-2` 实现暂停 / 恢复监听与事件广播

**状态**：`✅ 已完成`

**完成内容**：
- 后端新增 `set_monitoring` IPC 命令，并在 `enabled=true` 时通过 `sync_clipboard_state()` 避免暂停期间复制内容在恢复后被误入库
- 新增 `system:monitoring-changed` 事件广播，托盘状态刷新也已接入同一条状态变更链路
- 前端 `src/api/commands.ts` 与 `src/api/events.ts` 已补齐监听状态切换命令和事件订阅封装，测试桩同步更新

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml monitor::` ✅
- `pnpm run test src/__tests__/api/commands.test.ts src/__tests__/api/events.test.ts` ✅

**下一任务**：
- 领取 `TASK-04-3`：增加监听暂停提示、清空历史成功 / 失败反馈

### 2026-03-07：完成 `TASK-02-1` 实现托盘显示 / 隐藏 / 退出

**状态**：`✅ 已完成`

**完成内容**：
- 将 `src-tauri/src/tray/mod.rs` 扩展为可初始化的 `TrayController`，在应用启动阶段创建系统托盘与菜单
- 接通托盘菜单“显示主面板 / 隐藏主面板”“退出应用”的真实事件路由，并支持托盘菜单状态刷新
- 顺带接入“打开日志目录”菜单动作，为后续错误排查提供快捷入口

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml tray::` ✅

**下一任务**：
- 领取 `TASK-02-2`：实现暂停 / 恢复监听与事件广播

### 2026-03-07：完成 `TASK-01-3` 设计托盘状态与图标资源

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/tray/mod.rs`，定义托盘状态枚举、运行态快照和后续菜单项 ID 常量
- 新增 `src-tauri/icons/tray-active.png` 与 `src-tauri/icons/tray-paused.png` 两套托盘图标资源，并在代码中以嵌入资源方式引用
- 补齐托盘状态文案与图标资源的单元测试，为下一步托盘菜单行为实现做好基座

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml tray::` ✅

**下一任务**：
- 领取 `TASK-02-1`：实现托盘显示 / 隐藏 / 退出

### 2026-03-07：完成 `TASK-01-2` 实现 macOS Launch Agent 服务

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/autostart/mod.rs`，实现 Launch Agent plist 的生成、查询、删除与幂等修复逻辑
- 在 `src-tauri/src/lib.rs` 启动流程中接入 `LaunchAgentService`，按 `config.json.launch_at_login` 执行弱修复
- 扩展 `src-tauri/src/state.rs` 与 `src-tauri/src/error.rs`，为后续运行态查询、自启动切换和错误分类打基础

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml autostart::` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml config::` ✅

**下一任务**：
- 领取 `TASK-01-3`：设计托盘状态与图标资源

### 2026-03-07：完成 `TASK-01-1` 扩展 `config.json` 支持 `launch_at_login`

**状态**：`✅ 已完成`

**完成内容**：
- 为 `src-tauri/src/config/schema.rs` 的 `AppConfig` 新增 `launch_at_login` 字段，默认值按 v0.3 契约回填为 `true`
- 为 `src-tauri/src/config/mod.rs` 新增 `ConfigStore`，支持读取配置快照与持久化更新 `launch_at_login`
- 补齐旧配置自动回填与持久化写回测试，保证 v0.2 升级到 v0.3 时无需手工重建配置文件

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml config::` ✅

**下一任务**：
- 领取 `TASK-01-2`：实现 macOS Launch Agent 服务

### 2026-03-07：完成 `TASK-00-1` 建立 v0.3 文档基线

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `docs/v0.3/` 目录，并编写用户故事、DDD、数据库、自动化测试、BDD、API、状态码、种子数据、目录结构、页面地图、UX、冲刺任务等文档
- 明确 `v0.3` 只聚焦系统托盘、开机自启动、暂停 / 恢复监听、数字键快选、多显示器、动效与清空历史
- 补充 `docs/项目决策记录文档.md` 与 `docs/v0.3/版本迭代日志记录.md` 的基线记录

**下一任务**：
- 领取 `TASK-01-1`：扩展 `config.json` 支持 `launch_at_login`
