# v1.0 版本迭代日志记录

> 更新时间：2026-03-08
> 分支：`main`
> 当前阶段：v1.0 冲刺已完成（13/13 任务完成）

---

## 1. 迭代总览

| 版本标记 | 日期 | 状态 | 完成度 | 说明 |
|---|---|---|---|---|
| v1.0.0-release-readiness.1 | 2026-03-08 | 已完成 | 13/13 | 完成三平台打包、签名、发布清单与回滚收口 |
| v1.0.0-automation-regression.1 | 2026-03-08 | 已完成 | 12/13 | 补齐 v1.0 前后端与 BDD 自动化回归 |
| v1.0.0-orphan-cleanup.1 | 2026-03-08 | 已完成 | 11/13 | 完成孤立图片清理命令、周期清理与诊断摘要回写 |
| v1.0.0-image-cache.1 | 2026-03-08 | 已完成 | 10/13 | 完成图片原图 LRU 缓存、缓存驱逐与最近历史摘要查询优化 |
| v1.0.0-virtual-list.1 | 2026-03-08 | 已完成 | 9/13 | 完成主面板虚拟滚动与键盘交互稳定性收口 |
| v1.0.0-update.1 | 2026-03-08 | 已完成 | 8/13 | 完成应用内手动检查更新、三态结果反馈与下载入口 |
| v1.0.0-permission.2 | 2026-03-08 | 已完成 | 7/13 | 完成权限引导弹窗、状态条与重试闭环 |
| v1.0.0-permission.1 | 2026-03-08 | 已完成 | 6/13 | 完成 `macOS` 辅助功能权限检测与系统设置跳转命令 |
| v1.0.0-about.1 | 2026-03-08 | 已完成 | 5/13 | 完成关于页独立窗口、托盘 / 主面板入口与统一激活策略 |
| v1.0.0-diagnostics.1 | 2026-03-08 | 已完成 | 4/13 | 完成 `ReleaseInfo` / `DiagnosticsSnapshot` 读模型与前后端查询接口 |
| v1.0.0-db.1 | 2026-03-08 | 已完成 | 3/13 | 完成 `SQLite` 迁移摘要、损坏恢复备份回写与启动摘要落盘 |
| v1.0.0-log.1 | 2026-03-08 | 已完成 | 2/13 | 完成统一日志初始化、前端日志桥接与日志目录读取链路 |
| v1.0.0-doc.1 | 2026-03-08 | 已完成 | 1/13 | 建立 `v1.0` 全套设计、契约、页面、UX、测试与冲刺文档，锁定当前版本范围 |

---

## 2. 本次变更明细

### 2.1 2026-03-08 `v1.0-orphan-cleanup-diagnostics`

**变更类型**：`feat(cleanup)`

**主要变更**：
- 新增 `src-tauri/src/image/cleanup.rs`，统一实现孤立原图 / 缩略图扫描、物理删除、最近一次清理摘要回写与结构化日志输出
- IPC 新增 `run_orphan_cleanup`，并通过 `system:diagnostics-updated` 事件把最新 `DiagnosticsSnapshot` 推送给前端
- `src-tauri/src/lib.rs` 已接入每 `6` 小时一次的后台周期清理任务，和手动命令复用同一套 `ImageCleanupService`
- 关于页新增“清理冗余文件”入口、最近清理摘要与失败反馈，手动执行后可立即看到删除统计
- 前后端自动化测试已覆盖清理成功 / 失败反馈、诊断事件订阅与诊断快照中的 `last_orphan_cleanup`

**产出结果**：
- 图片目录现在具备“数据库对比扫描 → 安全删除 → 统计回写 → 诊断刷新”的完整治理闭环
- 诊断快照不再只有迁移与权限信息，也能向关于页实时回显最近一次孤立文件清理结果
- 第 3 批“性能与存储治理”任务已全部完成，后续可以进入 `TASK-04-1` 的自动化回归收口阶段

**验证结果**：
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

### 2.2 2026-03-08 `v1.0-image-cache-query-optimization`

**变更类型**：`feat(image)`

**主要变更**：
- `src-tauri/src/image/storage.rs` 新增原图 `LRU` 缓存，采用 `32` 条目上限与 `64MB` 字节预算双阈值控制，并在缓存命中时跳过磁盘读取与解码
- `remove_assets` 删除原图 / 缩略图时会同步驱逐对应缓存项，避免图片文件已清理但内存仍保留旧资源
- `src-tauri/src/persistence/sqlite.rs` 的 `SUMMARY_SELECT_SQL` 已改为在 SQL 层直接计算 `text_char_count / text_line_count`，不再把完整 `text_content` 带回最近历史摘要查询
- `src-tauri/src/persistence/row_mapper.rs` 已改为消费新的摘要列结构，并补上空文本 `TextMeta` 的兼容处理
- 新增 `LRU` 命中 / 淘汰 / 驱逐与空文本摘要元数据测试，收口本任务的性能与一致性风险

**产出结果**：
- 高频图片粘贴场景下，原图恢复链路已经具备复用内存副本的能力，降低重复解码开销
- 最近历史摘要查询不再无意义搬运大段正文，为主面板 `200+` 记录场景继续降低 IO 与映射成本
- 图片文件删除与缓存生命周期已经对齐，为后续 `TASK-03-3` 的孤立文件清理打下基础

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml image::storage -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml list_record_summaries -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml list_recent_100_summaries_stays_under_80ms -- --nocapture` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

### 2.3 2026-03-08 `v1.0-main-panel-virtualization`

**变更类型**：`feat(panel)`

**主要变更**：
- `src/components/MainPanel/CardList.tsx` 已接入横向虚拟滚动，仅渲染可见区与缓冲区卡片，并在选中项变化时自动同步滚动位置
- `src/components/MainPanel/index.tsx` 初始化记录读取上限已提升到 `200`，为长列表场景提供足够数据量
- `src/stores/useClipboardStore.ts` 的运行态列表上限同步提升到 `200`，避免主面板刚进入虚拟滚动前就被前端状态层截断
- 新增 `src/__tests__/components/MainPanel.virtualization.test.tsx`，覆盖“大列表少渲染 + 键盘切换后粘贴/删除仍稳定”场景
- 原有主面板、键盘与应用级测试均已回归通过，确认虚拟滚动未破坏权限引导、删除、粘贴与清空链路

**产出结果**：
- 主面板在 `200+` 历史记录场景下开始具备可扩展的渲染性能基础
- 键盘左右切换、粘贴、删除仍然围绕同一份 `selectedIndex` 工作，没有因虚拟滚动丢失高亮状态
- 后续 `TASK-03-2` 可以在当前长列表基础上继续优化图片原图缓存与查询性能

**验证结果**：
- `pnpm test src/__tests__/App.test.tsx src/__tests__/App.permission.test.tsx src/__tests__/components/MainPanel.test.tsx src/__tests__/components/MainPanel.virtualization.test.tsx src/__tests__/hooks/useKeyboard.test.tsx src/__tests__/stores/useClipboardStore.test.ts` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅

### 2.4 2026-03-08 `v1.0-manual-update-feedback`

**变更类型**：`feat(updater)`

**主要变更**：
- 新增 `src-tauri/src/updater/mod.rs`，通过 `CLIPBOARD_UPDATE_FEED_URL` 指定更新源，统一输出 `UpdateCheckResult` 并兼容通用 JSON / GitHub Latest Release 两类响应格式
- IPC 新增 `check_app_update`，并广播 `system:update-check-finished` 事件，为关于页和后续自动化验收提供统一更新检查入口
- 前端新增 `src/api/updater.ts`、`onUpdateCheckFinished` 事件封装，以及关于页的“检查中 / 有更新 / 已最新 / 失败”结果卡片与下载入口
- 关于页已支持打开下载页、查看发行说明；更新检查失败时仅反馈当前结果，不阻塞日志目录和诊断信息继续使用
- 新增前端与后端自动化测试，覆盖 `available / latest / failed` 三态和事件回调

**产出结果**：
- 第 2 批“权限与更新”任务已全部完成
- 关于页已从只读诊断入口升级为包含手动更新检查的正式发布支持入口
- 后续 `TASK-03-1` 可专注主面板虚拟滚动与键盘交互稳定性优化

**验证结果**：
- `pnpm test src/__tests__/api/updater.test.ts src/__tests__/api/events.update.test.ts src/__tests__/components/AboutWindow.test.tsx src/__tests__/components/AboutWindow.update.test.tsx` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml updater -- --nocapture` ✅

### 2.5 2026-03-08 `v1.0-permission-guidance-loop`

**变更类型**：`feat(permission)`

**主要变更**：
- 新增 `src/components/common/PermissionGuideDialog.tsx`，提供“为什么需要权限”“如何前往系统设置”“授权后如何重试”的完整说明
- `src/App.tsx` 在启动与窗口重新聚焦时同步权限状态；`macOS` 权限缺失时自动打开引导弹窗，并支持“打开系统设置 / 重新检测”闭环
- `src/components/MainPanel/index.tsx` 增加权限状态提示条，显式标记受限能力，同时保留浏览、选择、删除等基础能力
- `src/hooks/useKeyboard.ts` 在权限缺失时阻止 `Enter / Shift+Enter` 粘贴并给出提示，不影响列表导航、快选与删除键
- 新增 `src/__tests__/App.permission.test.tsx`、`src/__tests__/components/MainPanel.permission.test.tsx`、`src/__tests__/hooks/useKeyboard.permission.test.tsx` 自动化测试

**产出结果**：
- 第 2 批权限引导能力已形成“检测 → 提示 → 跳转设置 → 重试恢复”的完整闭环
- `macOS` 权限缺失场景不再出现无反馈失败，用户可以继续浏览、选择、删除历史记录
- 后续 `TASK-02-3` 可以在现有关于页基础上继续接入手动检查更新与结果反馈

**验证结果**：
- `pnpm test src/__tests__/App.permission.test.tsx src/__tests__/components/MainPanel.permission.test.tsx src/__tests__/hooks/useKeyboard.permission.test.tsx src/__tests__/App.test.tsx src/__tests__/components/MainPanel.test.tsx src/__tests__/hooks/useKeyboard.test.tsx` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅

### 2.6 2026-03-08 `v1.0-macos-permission-probe`

**变更类型**：`feat(permission)`

**主要变更**：
- 平台层新增 `detect_accessibility_permission` 与 `open_accessibility_settings`，对 `macOS` 辅助功能权限进行原生探测与系统设置跳转
- `src-tauri/src/platform/macos.rs` 已接入 `AXIsProcessTrusted` 和 `x-apple.systempreferences` 跳转 URL
- IPC 新增 `get_permission_status`、`open_accessibility_settings`，前端 `src/api/diagnostics.ts` 已同步封装和测试
- 诊断读模型的 `PermissionStatus` 现已返回真实权限状态，不再固定为占位值

**产出结果**：
- 第 2 批权限引导闭环已经具备后端探测与系统设置跳转底座
- 下一任务可以直接基于现有接口实现主面板状态条、引导弹窗与重试流程
- 关于页和诊断读模型可以展示更可信的权限状态

**验证结果**：
- `pnpm run type-check` ✅
- `pnpm test src/__tests__/api/diagnostics.test.ts` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml accessibility_settings -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml diagnostics -- --nocapture` ✅

### 2.7 2026-03-08 `v1.0-about-window-entry`

**变更类型**：`feat(window)`

**主要变更**：
- 新增 `src-tauri/src/window/about_window.rs`，实现关于页窗口的单实例创建、恢复与聚焦
- IPC 新增 `show_about_window`，托盘菜单与主面板均已接入“关于”入口
- 前端新增 `src/components/AboutWindow.tsx`，展示版本信息、诊断摘要、日志目录与许可证说明
- `src/main.tsx` 已支持 `?window=about` 独立渲染关于页，更新检查区域先以占位结构保留给 `TASK-02-3`

**产出结果**：
- 第 1 批“发布底座”任务已全部完成
- 用户现在可以从托盘和主面板稳定进入关于页，不会重复创建多个窗口
- 关于页已经可以直接消费 `TASK-01-3` 的诊断读模型，为下一批更新检查与权限引导继续铺底

**验证结果**：
- `pnpm run type-check` ✅
- `pnpm test src/__tests__/api/diagnostics.test.ts src/__tests__/components/AboutWindow.test.tsx src/__tests__/components/MainPanel.test.tsx` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml about_window -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml diagnostics -- --nocapture` ✅

### 2.8 2026-03-08 `v1.0-diagnostics-read-model`

**变更类型**：`feat(diagnostics)`

**主要变更**：
- 新增 `src-tauri/src/diagnostics/mod.rs`，统一描述版本信息、权限状态、迁移摘要、日志目录与诊断快照
- 新增 `get_release_info`、`get_diagnostics_snapshot` IPC 命令，并在 `src-tauri/src/lib.rs` 完成命令注册
- 前端新增 `src/api/diagnostics.ts` 与 `src/__tests__/api/diagnostics.test.ts`，封装并验证诊断查询接口
- 前端类型定义补齐 `ReleaseInfo`、`PermissionStatus`、`MigrationStatus`、`CleanupSummary`、`DiagnosticsSnapshot`

**产出结果**：
- 关于页和诊断页已具备稳定的数据获取入口
- `TASK-01-2` 产出的迁移摘要已经接入正式读模型，不再只存在于启动日志
- 平台、会话类型、构建信息、配置版本与日志目录可通过一次调用聚合返回

**验证结果**：
- `pnpm run type-check` ✅
- `pnpm test src/__tests__/api/diagnostics.test.ts` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml diagnostics:: -- --nocapture` ✅

### 2.9 2026-03-08 `v1.0-db-recovery-summary`

**变更类型**：`feat(persistence)`

**主要变更**：
- 在 `src-tauri/src/persistence/mod.rs` 新增 `MigrationStatus` 与 `PersistenceInitialization`，统一描述本轮启动的迁移 / 恢复结果
- 在 `src-tauri/src/persistence/sqlite.rs` 为初始化流程补充 `migrated` 标记，并记录 `sqlite database initialized` 的迁移状态
- 启动阶段会输出 `sqlite startup summary`，包含当前 schema、是否迁移、是否损坏恢复、检查时间与备份路径
- 在 `src-tauri/src/state.rs`、`src-tauri/src/lib.rs` 中把迁移摘要注入全局状态，供后续诊断接口直接复用

**产出结果**：
- `SQLite` 已不只是“能恢复”，还会在启动后保留结构化恢复摘要
- 后续 `ReleaseInfo / DiagnosticsSnapshot` 可以直接读取当前会话迁移状态，而不必重复扫描日志
- 自动化已覆盖“干净初始化”和“损坏恢复重建”两类发布高风险场景

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml initialize_with_recovery -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml initialize_database_creates_tables_indexes_and_schema_version -- --nocapture` ✅

### 2.10 2026-03-08 `v1.0-log-bridge`

**变更类型**：`feat(logging)`

**主要变更**：
- 启用 `src-tauri/src/logging.rs` 的统一日志初始化，支持控制台、滚动文件与 `panic` 捕获
- 通过 `src-tauri/src/ipc/commands.rs` 暴露 `write_client_log`、`get_log_directory`，供前端桥接调用
- 在 `src/api/logger.ts`、`src/api/commands.ts`、`src/api/events.ts`、`src/main.tsx` 等前端关键链路接入结构化日志上报
- 托盘菜单提供“打开日志目录”动作，便于人工验收与问题排查

**产出结果**：
- 前后端错误已可落到同一轮会话的本地日志目录中
- 前端日志会自动做上下文清洗与消息截断，降低敏感信息与超长日志风险
- 日志目录可以通过 IPC 查询，也可以从托盘直接打开

**验证结果**：
- `pnpm test src/__tests__/api/logger.test.ts src/__tests__/api/commands.test.ts src/__tests__/api/events.test.ts` ✅

### 2.11 2026-03-08 `v1.0-doc-baseline`

**变更类型**：`docs(v1.0)`

**主要变更**：
- 新增 `docs/v1.0/用户故事文档.md`
- 新增 `docs/v1.0/DDD战术设计文档.md`
- 新增 `docs/v1.0/数据库设计文档.md`
- 新增 `docs/v1.0/自动化测试文档.md`
- 新增 `docs/v1.0/BDD自动化验收文档.md`
- 新增 `docs/v1.0/API契约文档.md`
- 新增 `docs/v1.0/状态码契约文档.md`
- 新增 `docs/v1.0/种子数据契约文档.md`
- 新增 `docs/v1.0/文件目录结构设计文档.md`
- 新增 `docs/v1.0/页面地图与每个页面的详细功能描述文档.md`
- 新增 `docs/v1.0/UX设计文档.md`
- 新增 `docs/v1.0/冲刺任务文档.md`
- 更新 `docs/项目决策记录文档.md`，锁定 `v1.0` 版本边界、存储演进与信息架构方向

**产出结果**：
- `v1.0` 当前版本范围已收敛为“发布质量 + 性能优化 + 权限引导 + 关于与诊断”。
- 文档边界与 `docs/迭代版本规划文档.md` 中的 `v1.0 Release` 目标保持一致。
- `SQLite`、日志目录、关于页、权限引导、更新检查与自动化验收的设计基线已经对齐。

**验证结果**：
- 文档结构与命名对齐 `docs/v0.1` ~ `docs/v0.5` 现有范式 ✅
- `v1.0` 版本目标与 `docs/迭代版本规划文档.md` 保持一致 ✅

---

### 2.12 2026-03-08 `v1.0-automation-regression`

**变更类型**：`test(e2e)`

**主要变更**：
- `e2e/support/tauriMock.js` 已补齐 `v1.0` 所需的版本信息、诊断快照、权限状态、更新检查、关于页与孤立图片清理命令 mock，并新增 `system:diagnostics-updated` 事件侧写
- `e2e/tests/bdd.spec.ts` 已扩展场景构建器、类型定义与辅助方法，便于按场景注入 `ReleaseInfo / DiagnosticsSnapshot / PermissionStatus / UpdateCheckResult / CleanupSummary`
- 新增 `v1.0` BDD 回归场景，覆盖清空历史一致性、`macOS` 权限缺失与恢复、关于页版本信息、新版本提示、更新失败降级、虚拟滚动稳定性与孤立图片清理
- 修正 E2E mock 中“当前诊断快照”和“下次手动清理返回值”的状态边界，避免关于页初始摘要被预置清理结果污染
- 本轮已将 Playwright BDD、前端覆盖率测试、前端静态检查与 Rust 单元测试串成统一验收链路

**产出结果**：
- 第 4 批自动化回归能力已补齐，`v1.0` 关键发布链路现在拥有可重复执行的自动化验收基线
- 关于页、权限引导、更新检查与长列表交互等高风险场景已纳入回归范围，后续打包前可直接复用现有命令做发布前验收
- 冲刺剩余任务收敛为 `TASK-04-2`，后续可专注三平台打包、签名与发布清单整理

**验证结果**：
- `pnpm run test:e2e` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

---

### 2.13 2026-03-08 `v1.0-release-packaging-readiness`

**变更类型**：`chore(release)`

**主要变更**：
- 统一将 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 版本提升到 `1.0.0`，并补齐正式发布所需的产品名、描述与打包元数据
- 新增 `scripts/sign-windows.ps1`，让 Windows 发布构建通过 Tauri `signCommand` 调用 `signtool`，可从 GitHub Secrets 导入证书后完成签名
- 新增 `scripts/package-macos-dmg.sh`，改用 `hdiutil` 进行无界面 `DMG` 封装，避免 `create-dmg` 的 Finder AppleScript 在终端 / CI 环境中阻塞
- 新增 `scripts/verify-release.mjs`、`release:check` 与 `release:build:local`，将版本一致性校验、全量自动化回归与本机无签名打包冒烟收敛为统一发布前检查入口
- 更新 `.github/workflows/ci.yml` 与新增 `.github/workflows/release.yml`，分别覆盖三平台无签名打包冒烟、tag 触发的正式打包 / 签名 / `SHA256SUMS.txt` / Draft Release 上传
- 新增 `docs/v1.0/发布清单与回滚说明.md`，沉淀发布 secrets、执行清单、产物范围和回滚说明

**产出结果**：
- `v1.0` 最后一项“发布收口”任务已完成，仓库现在具备从 tag 到 Draft Release 的可重复执行流程
- 日常 `CI` 与正式 `Release` 流水线职责已经分离：前者做无签名三平台冒烟，后者做签名、产物汇总与 Draft Release 发布
- macOS `DMG` 打包从易卡住的 Finder 自动化切换到无界面脚本，显著降低发布链路在终端 / CI 环境中的不确定性

**验证结果**：
- `pnpm run release:verify-config` ✅
- `pnpm run release:check` ✅
- `pnpm run release:build:local` ✅

---

## 3. 当前阶段结论

### 已锁定事项

- `v1.0` 不是新业务功能扩展版本，而是正式发布收口版本。
- 当前优先级以 `P0` 的迁移恢复、日志诊断、权限引导、性能稳定性为主。
- `v1.0` 保持 `v0.5` 的设置结构与三表业务模型稳定，不再新增业务表。

### 下一步建议

1. 按 `docs/v1.0/发布清单与回滚说明.md` 执行正式 tag、Release workflow 与 Draft Release 核对。
2. 发布后一周重点观察日志、更新源与权限引导反馈，确认 `v1.0` 稳定性目标达成。
3. 基于真实用户反馈再启动 `v2.0` 的搜索、收藏、标签与同步规划。
