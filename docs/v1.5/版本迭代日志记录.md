# v1.5 版本迭代日志记录

## 2026-03-15

### 已完成

- 完成图片完整预览质量修正：独立预览窗口与历史覆盖层预览统一只使用原图资源，不再在完整预览中回退到缩略图，避免放大查看时看到压缩后的预览图
- 完成网址预览极简收口：链接预览头部进一步压缩为单行地址栏样式，仅保留站点、地址与打开按钮
- 完成网址预览头部收紧：进一步压缩链接预览信息栏尺寸，去掉标题与地址重复展示，首屏只保留一份核心 URL 信息
- 完成网址预览体验重构：链接记录改为在预览窗口内直接内嵌网页页面，默认优先展示实际页面内容，不再以站点封面、简介和抓取摘要作为主视觉
- 完成网址预览性能收敛：链接预览不再为了抓取简介而阻塞首屏展示，只保留精简的网址信息栏与页面内容区
- 完成预览音效收敛：取消预览窗口首次成功显示时的提示音，仅保留复制成功与粘贴成功音效，避免连续预览媒体内容时过于吵闹
- 完成媒体预览体验收敛：音频 / 视频预览改为更精简紧凑的播放器优先布局，弱化冗长详情区，让窗口打开后直接聚焦播放内容
- 完成媒体播放体验升级：音频 / 视频播放器默认自动播放，并把独立预览窗口基础尺寸收紧到更贴近媒体消费的紧凑规格
- 完成本地媒体预览权限修正：扩展 Tauri `assetProtocol.scope` 到用户目录与临时目录，修复独立预览窗口无法读取音频 / 视频 / 文稿源文件、误报“源不可用或环境无法解码”的问题
- 完成媒体预览状态修正：音频 / 视频文件记录改为默认直接进入可渲染状态，不再错误写入长期停留的 `pending`
- 完成媒体预览兼容修正：详情读取阶段会把历史遗留的 `audio / video pending` 记录自动提升为 `ready`，修复预览窗口一直显示“正在准备音频预览，请稍候”的问题
- 完成媒体预览回归测试：新增 SQLite 详情映射测试，覆盖历史 `pending` 音频 / 视频记录的自动修正场景
- 完成预览回归修正：移除主面板内旧 `PreviewOverlay` 的实际渲染，`v1.5` 预览链路统一回到独立 `PreviewWindow`，避免打开预览时出现“窗口 + 面板覆盖层”双层重复预览
- 完成预览回归修正：保留 `previewOverlay` 运行态作为“预览中高亮 / 关闭快捷键 / 跟随选中联动”的轻量会话状态，并补充主面板测试，防止旧 overlay 再次被误挂载
- 完成 `TASK-15-03-1`：扩展前后端 `ClipboardRecordDetail` 共享契约，补齐 `preview_status`、`preview_renderer`、`primary_uri` 与 `audio / video / document / link` 多类型预览详情结构
- 完成 `TASK-15-03-1`：新增 SQLite schema v4 迁移，为 `clipboard_items` 增加预览状态字段，并新增 `preview_assets` 通用预览资源表与索引
- 完成 `TASK-15-03-1`：调整运行时入库与详情映射逻辑，使文本 / 链接 / 图片 / 文件类记录能回填推荐渲染器、预览状态与主资源 URI，为后续媒体 / 文稿 / 链接渲染器实现提供数据底座
- 完成 `TASK-15-03-2`：新增预览窗口音频渲染器，支持播放、暂停、进度拖动、基础文件信息展示与源文件缺失时的降级态
- 完成 `TASK-15-03-2`：修正预览窗口在媒体记录上的快捷键边界，播放器聚焦时空格键不再误关闭窗口，预览成功音效仅在可渲染内容真正 ready 后触发
- 完成 `TASK-15-03-2`：补齐音频预览前端测试与 Rust 详情映射测试，并顺手修复 `v1.5` 契约扩展后遗留的测试初始化字段缺失问题
- 完成 `TASK-15-03-3`：新增预览窗口视频渲染器，支持播放、暂停、进度拖动、时长 / 分辨率展示、封面透传与媒体源失效降级态
- 完成 `TASK-15-03-3`：补齐视频预览前端测试与 Rust 详情映射测试，验证播放器渲染、封面回显、元信息更新与失败回退
- 完成 `TASK-15-04-1`：新增 PDF 预览渲染器，支持正文容器展示、页数信息回显与文件路径级降级态
- 完成 `TASK-15-04-1`：修正 PDF 详情状态推导，已存在源文件的 `pdf` 记录会在详情层提升为可渲染状态，并补齐对应 Rust 回归测试
- 完成 `TASK-15-04-2`：新增 `prepare_record_preview` 按需预览准备命令，`DOCX / XLSX / PPTX` 采用轻量 `ZIP/XML` 结构化提取生成 `document_outline / document_text` 资源
- 完成 `TASK-15-04-2`：新增预览窗口 `DocumentPreview` 渲染器，支持正文片段、工作表摘要、幻灯片摘要展示，以及旧版 Office 文稿 `unsupported` 降级态
- 完成 `TASK-15-04-3`：新增链接预览准备链路，基于轻量 HTML 元信息抓取生成标题、站点名、摘要、正文片段与抓取时间
- 完成 `TASK-15-04-3`：新增预览窗口 `LinkPreview` 渲染器，支持 URL、标题、摘要、站点信息、外部打开入口与失败降级提示
- 完成 `TASK-15-05-1`：补齐 Office / 链接预览前端单元测试、Rust 预览准备测试与 Playwright BDD 场景
- 完成 `TASK-15-05-1`：修复覆盖率门槛回归，新增文稿准备中与链接外部打开失败分支测试，恢复全局分支覆盖率到阈值以上
- 完成测试稳定性修正：放宽缩略图性能哨兵阈值，降低 `cargo test` 在本地高负载环境下的误报概率

### 测试与验收

- 通过 `pnpm run lint`
- 通过 `pnpm run type-check`
- 通过 `pnpm run test:coverage`
- 通过 `cargo test --manifest-path src-tauri/Cargo.toml`
- 通过 `pnpm run test:e2e`

### 备注

- `v1.5` 冲刺范围内的音效反馈、预览联动、音频 / 视频 / PDF / Office / 链接预览与专项回归测试已全部完成
- 预览准备当前采用“前端读取详情时按需触发 `prepare_record_preview`，随后回读最终详情”的同步式用户体验，不依赖额外后台事件即可拿到可渲染内容
- `src-tauri/Cargo.toml` 已预置 `regex`、`quick-xml`、`zip` 依赖，为后续文稿结构化解析实现做准备
- 由于当前 `React 19` 与公开 `@vidstack/react` 版本兼容边界不够明确，媒体预览首版先采用原生 `HTMLMediaElement` 落地，后续是否切换统一播放器库另行评估

## 2026-03-14

### 已完成

- 完成 `TASK-15-01-1`：将 `docs/音效/` 下 3 个原始素材复制到 `src/assets/sounds/`，统一改名为 `copy-notification.mp3`、`paste-notification.mp3`、`preview-open.mp3`
- 完成 `TASK-15-01-2`：新增前端 `SoundEffectService`，基于 `Howler.js` 在复制成功、粘贴成功、预览首次显示成功时播放音效，并在失败时静默降级
- 完成 `TASK-15-02-1`：补齐预览联动状态模型，为预览会话增加 `followSelection` 语义，并把“显式打开”和“静默联动”拆分
- 完成 `TASK-15-02-2`：新增轻量 IPC `sync_preview_window_record`，主面板在预览窗口打开后可跟随当前选中卡片同步切换预览内容，不再复用会抢焦点的打开命令

### 测试与验收

- 通过 `pnpm run lint`
- 通过 `pnpm run type-check`
- 通过 `pnpm run test:coverage`
- 通过 `cargo test --manifest-path src-tauri/Cargo.toml`

### 备注

- 当前版本的短音效播放先由前端统一音效服务承担，Rust 继续只负责成功事件源与窗口同步能力
- 预览窗口打开后仍会阻止 `Enter / Delete` 这类破坏性主列表动作，但允许方向键和数字快选继续驱动联动浏览

### 进度同步

- 当前阶段成果已完成文档同步，并已提交到 Git
- 当前代码检查点提交为 `0d42eb6`（`feat(preview): 增加音效反馈与预览联动`）
- 下一阶段从 `TASK-15-03-1` 开始推进多类型预览契约与资源模型扩展
