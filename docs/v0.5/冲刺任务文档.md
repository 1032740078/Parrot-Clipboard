# v0.5 冲刺任务文档

> 版本阶段：v0.5 Beta
> 当前状态：第 4 批进行中，已完成 15/19 个任务

## 1. 冲刺目标

在 `v0.3` 单平台基线上完成：

- Windows / Linux X11 主流程适配
- 设置窗口与配置持久化升级
- 自定义快捷键与冲突检测
- 黑名单应用过滤
- Wayland 能力降级提示

---

## 2. 任务总览

| 任务 ID | 说明 | 批次 | 优先级 | 状态 |
|---------|------|------|--------|------|
| `TASK-00-1` | 建立 v0.5 文档、契约、页面、UX、冲刺与迭代日志 | 第 0 批 | P0 | `✅ 已完成` |
| `TASK-01-1` | 设计 `config.json v2` 分组结构与迁移逻辑 | 第 1 批 | P0 | `✅ 已完成` |
| `TASK-01-2` | 建立 `settings/` 模块与设置聚合校验 | 第 1 批 | P0 | `✅ 已完成` |
| `TASK-01-3` | 扩展平台能力探测模型与 `get_platform_capabilities` | 第 1 批 | P0 | `✅ 已完成` |
| `TASK-01-4` | 新增设置窗口单实例打开 / 激活能力 | 第 1 批 | P0 | `✅ 已完成` |
| `TASK-02-1` | 完成 Windows 粘贴板、快捷键、自启动适配 | 第 2 批 | P0 | `✅ 已完成` |
| `TASK-02-2` | 完成 Linux X11 粘贴板、快捷键、自启动适配 | 第 2 批 | P0 | `✅ 已完成` |
| `TASK-02-3` | 完成 Wayland 会话探测与降级提示策略 | 第 2 批 | P0 | `✅ 已完成` |
| `TASK-02-4` | 将平台能力接入运行态与托盘 / 设置页 | 第 2 批 | P1 | `✅ 已完成` |
| `TASK-03-1` | 搭建设置窗口导航与分组容器 | 第 3 批 | P0 | `✅ 已完成` |
| `TASK-03-2` | 完成通用页与记录页表单 | 第 3 批 | P0 | `✅ 已完成` |
| `TASK-03-3` | 完成快捷键录制器、冲突校验与恢复默认 | 第 3 批 | P0 | `✅ 已完成` |
| `TASK-03-4` | 完成隐私页黑名单规则列表与表单 | 第 3 批 | P0 | `✅ 已完成` |
| `TASK-04-1` | 新增设置相关 IPC 命令、事件与前端 API 封装 | 第 4 批 | P0 | `✅ 已完成` |
| `TASK-04-2` | 打通主题同步与多入口状态一致性 | 第 4 批 | P1 | `✅ 已完成` |
| `TASK-04-3` | 打通黑名单过滤到监听链路 | 第 4 批 | P0 | `⬜ 未开始` |
| `TASK-05-1` | 补齐 Rust 单元 / 集成测试 | 第 5 批 | P0 | `⬜ 未开始` |
| `TASK-05-2` | 补齐前端设置页与主题测试 | 第 5 批 | P0 | `⬜ 未开始` |
| `TASK-05-3` | 建立 BDD / E2E 验收场景并回归 | 第 5 批 | P0 | `⬜ 未开始` |

---

## 3. 各批次任务详情

### 3.1 第 0 批：文档基线

| 任务 ID | 说明 | 交付物 | 状态 |
|---------|------|--------|------|
| `TASK-00-1` | 输出 v0.5 用户故事、DDD、数据库、API、状态码、种子数据、页面地图、UX、测试、冲刺和版本日志 | `docs/v0.5/*.md` | `✅ 已完成` |

### 3.2 第 1 批：设置模型与平台能力基建

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-01-1` | 配置从扁平结构迁移到 `config_version=2` 的分组结构 | 无 | `✅ 已完成` |
| `TASK-01-2` | 新增设置聚合、校验器与黑名单规则模型 | `TASK-01-1` | `✅ 已完成` |
| `TASK-01-3` | 抽象平台能力矩阵与会话类型识别 | 无 | `✅ 已完成` |
| `TASK-01-4` | 建立设置窗口单实例管理与激活策略 | 无 | `✅ 已完成` |

### 3.3 第 2 批：跨平台能力落地

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-02-1` | Windows 平台适配：监听、快捷键、自启动、粘贴模拟 | `TASK-01-3` | `✅ 已完成` |
| `TASK-02-2` | Linux X11 平台适配：监听、快捷键、自启动、粘贴模拟 | `TASK-01-3` | `✅ 已完成` |
| `TASK-02-3` | Wayland 不支持项的探测、状态码与提示文案 | `TASK-01-3` | `✅ 已完成` |
| `TASK-02-4` | 平台能力状态与托盘 / 设置页联动 | `TASK-01-4`、`TASK-02-3` | `✅ 已完成` |

### 3.4 第 3 批：设置窗口实现

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-03-1` | 实现设置窗口导航、壳层、未保存确认 | `TASK-01-4` | `✅ 已完成` |
| `TASK-03-2` | 实现通用页和记录页表单保存 | `TASK-01-2` | `✅ 已完成` |
| `TASK-03-3` | 实现快捷键录制器、冲突校验与保存 | `TASK-01-3`、`TASK-04-1` | `✅ 已完成` |
| `TASK-03-4` | 实现隐私页黑名单列表、表单与删除流程 | `TASK-01-2`、`TASK-04-1` | `✅ 已完成` |

### 3.5 第 4 批：系统同步与监听链路

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-04-1` | 新增设置相关 IPC / Event / 前端 API | 第 1 批完成 | `✅ 已完成` |
| `TASK-04-2` | 主题同步到主面板与设置窗口 | `TASK-03-2` | `✅ 已完成` |
| `TASK-04-3` | 黑名单规则接入监听过滤链路 | `TASK-03-4` | `⬜ 未开始` |

### 3.6 第 5 批：自动化测试与验收

| 任务 ID | 说明 | 依赖 | 状态 |
|---------|------|------|------|
| `TASK-05-1` | Rust 单元 / 集成测试覆盖迁移、黑名单、平台能力、快捷键 | 前 4 批核心功能完成 | `⬜ 未开始` |
| `TASK-05-2` | 前端测试覆盖设置页、主题、冲突提示、降级提示 | 前 4 批前端功能完成 | `⬜ 未开始` |
| `TASK-05-3` | BDD / E2E 验收与回归 | `TASK-05-1`、`TASK-05-2` | `⬜ 未开始` |

---

## 4. 风险与缓解

| 风险 | 说明 | 缓解方案 |
|------|------|----------|
| 平台 API 差异大 | Windows / Linux 与 macOS 实现差异明显 | 先统一能力矩阵，再按平台实现 |
| Wayland 不稳定 | 全局快捷键与活动应用识别能力受限 | 明确降级文案，不做假支持 |
| 配置迁移破坏旧用户 | 旧扁平配置升级为新结构有风险 | 单独做迁移单元与集成测试 |
| 设置同步不一致 | 主面板、托盘、设置页可能显示不同状态 | 引入统一 `settings-updated` 事件 |

---

## 5. 冲刺完成后建议

- `v0.5` 完成后优先进入跨平台真机回归，而不是继续扩搜索 / 收藏等新需求
- 若 Wayland 降级体验仍不足，可在后续版本继续增强替代入口与引导文案
- 待 `v0.5` 稳定后，再规划 `v1.0` 的性能、发布收敛与完整用户文档

---

## 6. 最新进展

### 2026-03-07：完成 `TASK-04-2` 主题同步与多入口状态一致性

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `useSettingsStore` 与 `useThemeSync`，主面板启动时会读取 `get_settings_snapshot`，并把主题模式统一同步到 `document.documentElement.dataset.theme`
- `useSystemEvents` 接入 `system:settings-updated` 与 `system:launch-at-login-changed`，确保主面板、设置窗口与运行态中的主题 / 自启动状态保持一致
- 设置窗口与主面板统一改用基于 CSS 变量的主题色板与阴影令牌，浅色 / 深色模式在两个入口下保持同一套视觉基线
- 前端补充 `useThemeSync`、`useSystemEvents`、`App` 相关测试，覆盖启动拉取设置、事件驱动切换主题与状态同步

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

**下一任务**：
- 领取 `TASK-04-3`：打通黑名单过滤到监听链路

### 2026-03-07：完成 `TASK-03-4` 隐私页黑名单规则列表与表单

**状态**：`✅ 已完成`

**完成内容**：
- 隐私页由占位壳层升级为真实规则管理界面，支持黑名单列表展示、空态提示、编辑入口、启停切换与删除操作
- 新增黑名单表单支持应用名称、目标平台、匹配类型、应用标识填写，并复用顶部保存按钮完成新增 / 编辑保存
- 设置窗口关闭与分组切换现在会感知隐私表单未保存状态，重复规则错误也能以内联提示与 Toast 双通道反馈
- 前端补充 4 个隐私页用例，覆盖列表渲染、新增、编辑、停用、删除以及重复规则阻止新增

**自动化验证**：
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

**下一任务**：
- 领取 `TASK-04-2`：打通主题同步与多入口状态一致性

### 2026-03-07：完成 `TASK-04-1` 设置相关 IPC、事件与前端 API 封装

**状态**：`✅ 已完成`

**完成内容**：
- 后端补齐 `create_blacklist_rule`、`update_blacklist_rule`、`delete_blacklist_rule` 三个设置命令，并在设置保存后统一广播 `system:settings-updated`
- 通用设置保存时补充 `system:launch-at-login-changed` 事件，为托盘、主面板与设置窗口后续状态同步提供稳定消息源
- 前端 `settings.ts` 新增黑名单规则增删改 API，`events.ts` 新增设置更新与自启动变更订阅封装
- 同步补充 API 层自动化测试，并把黑名单重复错误信息调整为更可读的中文反馈

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml ipc::commands::tests -- --nocapture` ✅
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

**下一任务**：
- 领取 `TASK-03-4`：完成隐私页黑名单规则列表、表单、启停与删除交互

### 2026-03-07：完成 `TASK-03-3` 快捷键录制器、冲突校验与恢复默认

**状态**：`✅ 已完成`

**完成内容**：
- 后端新增 `validate_toggle_shortcut`、`update_toggle_shortcut` 与快捷键重注册能力，统一标准化录制结果并在保存后立即切换生效快捷键
- 设置窗口快捷键页完成录制态、校验态、恢复默认值与 Wayland 不支持禁用态，冲突组合会阻止保存并给出明确说明
- 前端补充快捷键设置 API 封装与 4 个核心交互用例，覆盖录制保存、冲突禁用、恢复默认与 Wayland 替代路径提示
- 修复 Tauri 快捷键字符串标准化差异，避免 `KeyV / Tab` 这类内部格式影响前后端展示与冲突判断

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml shortcut -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml ipc::commands::tests -- --nocapture` ✅
- `pnpm run lint` ✅
- `pnpm run type-check` ✅
- `pnpm run test:coverage` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

**下一任务**：
- 领取 `TASK-04-1`：补齐黑名单相关设置 IPC、事件与前端 API，作为隐私页表单前置链路

### 2026-03-07：完成 `TASK-03-2` 通用页与记录页表单

**状态**：`✅ 已完成`

**完成内容**：
- 后端新增 `get_settings_snapshot`、`update_general_settings`、`update_history_settings`，打通设置快照读取与通用 / 记录页保存最小链路
- 设置窗口初始化时回显真实配置快照，通用页支持主题与开机自启动保存，记录页支持容量与采集开关保存
- 保存成功后更新本地草稿基线并显示 Toast；校验失败时直接展示后端返回的可读错误信息
- 补充前端 `settings.ts` API 封装与对应测试，同时为设置窗口组件测试覆盖真实保存场景

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml settings:: -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml config:: -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml ipc::commands::tests -- --nocapture` ✅
- `pnpm run type-check` ✅
- `pnpm exec vitest run src/__tests__/api/settings.test.ts src/__tests__/api/errorHandler.test.ts src/__tests__/components/SettingsWindowPlaceholder.test.tsx` ✅

**下一任务**：
- 领取 `TASK-03-3`：完成快捷键录制器、冲突校验与恢复默认

### 2026-03-07：完成 `TASK-03-1` 设置窗口导航与分组容器

**状态**：`✅ 已完成`

**完成内容**：
- 设置窗口从占位页升级为真实壳层，完成“通用 / 记录与存储 / 快捷键 / 隐私”四个一级分组导航
- 右侧内容区按当前分组切换标题、描述与对应容器，为后续表单与保存链路预留稳定挂载点
- 通用分组新增本地草稿交互，并通过窗口关闭拦截与分组切换确认弹窗落实未保存确认流程
- Vitest 新增 Tauri `window` mock，覆盖设置窗口导航切换、未保存确认取消 / 放弃以及关闭请求拦截

**自动化验证**：
- `pnpm run type-check` ✅
- `pnpm exec vitest run src/__tests__/components/SettingsWindowPlaceholder.test.tsx` ✅

**下一任务**：
- 领取 `TASK-03-2`：完成通用页与记录页表单

### 2026-03-07：完成 `TASK-02-4` 平台能力接入运行态与托盘 / 设置页

**状态**：`✅ 已完成`

**完成内容**：
- 托盘菜单新增“打开设置”入口，并打通暂停监听、恢复监听、开机自启动切换的真实运行态联动
- 托盘初始化阶段接入平台能力矩阵；在 Wayland / 未知 Linux 会话下展示降级提示，指导用户改用托盘入口继续使用
- 设置窗口占位页接入 `get_platform_capabilities`，展示当前会话、能力状态卡片与降级说明
- 前端补充 `UNSUPPORTED_PLATFORM_FEATURE` 错误识别与平台能力 API 封装，避免把会话限制误判成未知错误

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml tray -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml config:: -- --nocapture` ✅
- `pnpm run type-check` ✅
- `pnpm exec vitest run src/__tests__/components/SettingsWindowPlaceholder.test.tsx src/__tests__/api/commands.test.ts src/__tests__/api/errorHandler.test.ts` ✅

**下一任务**：
- 领取 `TASK-03-1`：搭建设置窗口导航与分组容器

### 2026-03-07：完成 `TASK-02-3` Wayland 会话探测与降级提示策略

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `UNSUPPORTED_PLATFORM_FEATURE` 错误码映射，明确不再把 Wayland 的能力缺失混入通用内部错误
- Linux 运行时在 Wayland / 未知会话下改为返回 `UnsupportedKeySimulator`，禁止继续执行假粘贴
- 全局快捷键注册前先读取平台能力矩阵；当能力不是 `supported` 时跳过注册并写入结构化原因日志
- 补充快捷键注册策略测试，并回归平台能力解析测试确保 Wayland / X11 / 未知会话路径一致

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml shortcut -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml platform:: -- --nocapture` ✅

**下一任务**：
- 领取 `TASK-02-4`：将平台能力接入运行态与托盘 / 设置页

### 2026-03-07：完成 `TASK-02-2` Linux X11 平台粘贴板 / 快捷键 / 自启动适配

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/platform/linux.rs`，使用系统剪贴板与 `xdotool` 实现 Linux X11 粘贴恢复能力
- 新增 `src-tauri/src/autostart/linux.rs`，通过 `~/.config/autostart/*.desktop` 管理登录启动
- 应用启动阶段新增 Linux 分支，按平台工厂选择 Linux 剪贴板、按键模拟与自启动实现
- 补充 6 个 Rust 单元测试，覆盖 `xdotool` 参数与 XDG Autostart 文件创建 / 删除

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml linux -- --nocapture` ✅

**下一任务**：
- 领取 `TASK-02-3`：完成 Wayland 会话探测与降级提示策略

### 2026-03-07：完成 `TASK-02-1` Windows 平台粘贴板 / 快捷键 / 自启动适配

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/platform/windows.rs`，使用系统剪贴板与 `PowerShell SendKeys` 实现 Windows 粘贴恢复能力
- 新增 `src-tauri/src/autostart/windows.rs`，基于 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 管理开机自启动
- 后端运行时改为按平台选择剪贴板、自启动与按键模拟实现，Windows 分支不再复用 macOS 逻辑
- 补充 4 个 Rust 单元测试，覆盖注册表参数构造与 `Ctrl+V` 发送脚本

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml windows -- --nocapture` ✅

**下一任务**：
- 领取 `TASK-02-2`：完成 Linux X11 粘贴板、快捷键、自启动适配

### 2026-03-07：完成 `TASK-01-4` 设置窗口单实例打开 / 激活能力

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/window/settings_window.rs`，实现设置窗口动态创建、单实例复用、恢复显示与聚焦
- 新增后端 `show_settings_window` IPC 命令，并注册到 Tauri `invoke_handler`
- 前端增加 `window=settings` 占位页，避免设置窗口打开后仍渲染主面板内容
- 补充 3 个 Rust 单元测试与 1 个前端组件测试，覆盖单实例策略与占位页渲染

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml settings_window -- --nocapture` ✅
- `pnpm exec prettier --write src/main.tsx src/components/SettingsWindowPlaceholder.tsx src/__tests__/components/SettingsWindowPlaceholder.test.tsx` ✅
- `pnpm run type-check` ✅
- `pnpm exec vitest run src/__tests__/components/SettingsWindowPlaceholder.test.tsx` ✅

**下一任务**：
- 领取 `TASK-02-1`：完成 Windows 粘贴板、快捷键、自启动适配

### 2026-03-07：完成 `TASK-01-3` 平台能力矩阵与 `get_platform_capabilities`

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/platform/capabilities.rs`，建立 `CapabilityState`、`SessionType`、`PlatformCapabilities` 与 `PlatformCapabilityResolver`
- 支持按 `macOS / Windows / Linux X11 / Linux Wayland / Linux 未知会话` 生成统一能力矩阵
- 为 `Wayland` 定义结构化降级原因，并新增后端 `get_platform_capabilities` IPC 命令
- 补充 5 个 Rust 单元测试，覆盖三平台主场景、Wayland 降级与 Linux 未知会话兜底

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml platform:: -- --nocapture` ✅

**下一任务**：
- 领取 `TASK-01-4`：新增设置窗口单实例打开 / 激活能力

### 2026-03-07：完成 `TASK-01-2` 建立 `settings/` 聚合、校验器与黑名单规则模型

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `src-tauri/src/settings/` 模块，提供 `SettingsProfile` 聚合根、`SettingsValidationService` 校验服务与 `BlacklistRuleDraft` 草稿模型
- 建立通用设置、历史策略、快捷键与隐私黑名单的统一更新入口，为后续 IPC 和设置页保存链路打基础
- 增加黑名单规则新增、更新、删除与重复校验逻辑，重复判定基于 `platform + match_type + app_identifier`
- 补充 9 个 Rust 单元测试，覆盖设置校验、快捷键必填、黑名单归一化、去重与时间戳更新

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml settings:: -- --nocapture` ✅

**下一任务**：
- 领取 `TASK-01-3`：扩展平台能力探测模型与 `get_platform_capabilities`

### 2026-03-07：完成 `TASK-01-1` 落地 `config.json v2` 分组结构与兼容迁移

**状态**：`✅ 已完成`

**完成内容**：
- 将 `src-tauri/src/config/schema.rs` 从扁平配置升级为 `config_version=2` 的分组结构，新增 `general / history / shortcut / privacy` 配置分区
- 新增旧版扁平配置到 `v2` 的自动迁移能力，并在读取后原地回写标准化 JSON
- 增加损坏配置回退默认值并自愈重写的兜底逻辑，同时按平台提供默认快捷键
- 补充 7 个 Rust 单元测试，覆盖默认配置、旧版迁移、分组补齐、损坏配置恢复与持久化写回

**自动化验证**：
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml config:: -- --nocapture` ✅

**下一任务**：
- 领取 `TASK-01-2`：建立 `settings/` 模块与设置聚合校验

### 2026-03-07：完成 `TASK-00-1` 建立 v0.5 文档基线

**状态**：`✅ 已完成`

**完成内容**：
- 新增 `docs/v0.5/` 目录，并补齐用户故事、DDD、数据库、自动化测试、BDD、API、状态码、种子数据、目录结构、页面地图、UX、冲刺任务等文档
- 明确 `v0.5` 当前版本边界：三平台支持、设置窗口、快捷键冲突检测、黑名单规则、主题与容量配置
- 同步补充版本迭代日志与项目决策记录，锁定 `config.json v2` 与 Wayland 降级策略

**下一任务**：
- 领取 `TASK-01-1`：设计 `config.json v2` 分组结构与迁移逻辑
