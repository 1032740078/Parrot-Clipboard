# v1.1 冲刺任务文档

> 更新时间：2026-03-08
> 当前状态：已完成（6/6）
> 版本目标：修正主面板桌面交互体验，使“呼出 → 选择 → 快贴 / 粘贴 → 收起”链路更自然

## 1. 冲刺目标

- 完成主面板失焦自动隐藏
- 彻底移除“缩短主面板高度避让 `Dock`”方案，恢复 `220px` 原始高度
- 支持 `Command + 1~9` 直接粘贴
- 确保左右切换与数字快选命中不可见卡片时自动横向滚动

---

## 2. 任务总览

| 任务 ID | 任务名称 | 优先级 | 状态 |
|---------|----------|--------|------|
| `TASK-00-1` | 建立 `v1.1` 文档基线 | P0 | 已完成 |
| `TASK-01-1` | 收敛主面板显隐状态，落地失焦自动隐藏 | P0 | 已完成 |
| `TASK-01-2` | 调整 `macOS` 面板定位，恢复 `220px` 并覆盖 `Dock` | P0 | 已完成 |
| `TASK-02-1` | 实现 `Command + 1~9` 快速粘贴 | P0 | 已完成 |
| `TASK-02-2` | 补齐自动横向滚动与虚拟滚动稳定性 | P0 | 已完成 |
| `TASK-03-1` | 补齐前后端自动化回归与 BDD 验收 | P0 | 已完成 |

---

## 3. 各批次任务详情

### 3.1 第 0 批：文档基线

- 产出 `v1.1` 用户故事、DDD、数据库、测试、BDD、API、状态码、种子数据、目录设计、页面地图、UX、冲刺文档
- 更新项目决策记录，明确“覆盖 `Dock` 而非规避 `Dock`”

### 3.2 第 1 批：窗口与显隐收敛

- 原生窗口失焦时自动隐藏主面板
- 建立面板显隐变化事件，收敛前后端状态一致性
- 移除“窗口 focus 自动显示主面板”的错误依赖

### 3.3 第 2 批：交互效率增强

- 实现 `Command + 1~9` 快贴
- 保留 `1~9` 仅快选的既有语义
- 更新主面板底部快捷键提示

### 3.4 第 3 批：滚动与回归

- 小列表与虚拟滚动列表统一采用“选中项确保可见”策略
- 补齐前端单测、Rust 单测、BDD 自动化场景
- 验证 `Dock` 位置、多显示器和权限缺失组合场景

---

## 4. 风险与缓解

| 风险 | 说明 | 缓解方案 |
|------|------|----------|
| 面板显隐状态错位 | 后端已隐藏，前端仍认为可见 | 引入显隐变化事件，原生窗口状态作为真相源 |
| 覆盖 Dock 后层级不足 | 面板仍可能被系统 UI 压住 | 复用并校验高于 `Dock` 的原生层级策略 |
| 快贴和快选语义冲突 | `1~9` 与 `Command+1~9` 混淆 | 在键盘层显式区分修饰键，并补齐回归测试 |
| 自动滚动引发跳动 | 高频切换时滚动过度 | 仅滚动最小必要距离，不做强制居中 |

---

## 5. 冲刺完成后建议

1. 手测覆盖底部 / 左侧 / 右侧 `Dock`
2. 手测覆盖单屏、双屏、混合缩放
3. 若 `v1.1` 验收通过，再评估是否进入下一版本的搜索与管理能力规划

---

## 6. 最新进展

### 2026-03-08：完成 `TASK-03-1` 补齐前后端自动化回归与 BDD 验收

**变更类型**：`test(regression-bdd)`

**主要产出**：
- 增强 `e2e/support/tauriMock.js`，让 `hide_panel` 与 `system:panel-visibility-changed` 在 Mock 中同步 `panel_visible`
- 更新 `e2e/tests/bdd.spec.ts`，补齐 `BDD-11-01/02`、`BDD-13-01/02/03`、`BDD-14-01/02/03` 自动化场景
- 调整 `src/__tests__/App.permission.test.tsx` 权限重检测试桩，校准与当前权限流一致的回归预期
- 以 Rust 窗口定位回归承接 `BDD-12-01/02/03` 的 `Dock` / 多显示器原生验收

**自动化验证**：
- `pnpm run test:coverage` ✅
- `pnpm run test:e2e` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

**下一步**：
- `v1.1` P0 冲刺任务 `6/6` 已全部完成，可进入手测收口与发版准备

### 2026-03-08：完成 `TASK-02-2` 补齐自动横向滚动与虚拟滚动稳定性

**变更类型**：`feat(list-scroll)`

**主要产出**：
- 将卡片列表“选中项确保可见”滚动逻辑抽成统一 helper，保持最小必要滚动距离
- 将 `viewportWidth` 纳入滚动修正依赖，提升列表与虚拟列表切换时的稳定性
- 补齐小列表左右切换、虚拟列表左右切换、数字快选跳转后的自动横向滚动回归测试

**自动化验证**：
- `pnpm test src/__tests__/components/MainPanel.virtualization.test.tsx src/__tests__/hooks/useKeyboard.test.tsx` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅

**下一步**：
- 进入 `TASK-03-1`，统一补齐前后端自动化回归与 BDD 验收

### 2026-03-08：完成 `TASK-02-1` 实现 `Command + 1~9` 快速粘贴

**变更类型**：`feat(quick-paste)`

**主要产出**：
- 键盘层新增 `Command + 1~9` 快贴，仅在 `macOS` 启用
- 保留 `1~9` 单按仅快选的原有语义，不直接触发粘贴
- `Enter / Shift+Enter / Command+数字` 隐藏主面板时统一带上显隐 `reason`
- 主面板底部快捷键提示新增 `⌘+1-9 快贴`

**自动化验证**：
- `pnpm test src/__tests__/hooks/useKeyboard.test.tsx src/__tests__/hooks/useKeyboard.permission.test.tsx src/__tests__/components/MainPanel.test.tsx` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅

**下一步**：
- 进入 `TASK-02-2`，补齐自动横向滚动与虚拟滚动稳定性

### 2026-03-08：完成 `TASK-01-2` 调整 `macOS` 面板定位，恢复 `220px` 并覆盖 `Dock`

**变更类型**：`fix(window-position)`

**主要产出**：
- `macOS` 主面板定位改为读取 `NSScreen.frame`，不再使用 `visibleFrame`
- `macOS` fallback 路径也改为使用显示器物理 `position + size`，彻底移除 `work_area` 避让方案
- 补齐底部 / 左侧 / 右侧 `Dock` 与高度极限夹取的窗口 frame 单测

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml window::position -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml auto_hide -- --nocapture` ✅

**下一步**：
- 进入 `TASK-02-1`，实现 `Command + 1~9` 快速粘贴，并保持 `1~9` 仅快选

### 2026-03-08：完成 `TASK-01-1` 收敛主面板显隐状态，落地失焦自动隐藏

**变更类型**：`feat(panel-visibility)`

**主要产出**：
- 后端新增 `system:panel-visibility-changed` 事件，统一广播主面板显示 / 隐藏状态
- 主窗口注册失焦自动隐藏逻辑，并在快捷键、托盘、`Esc`、粘贴成功路径补齐显隐同步
- 前端移除浏览器 `focus / pageshow / visibilitychange` 自动显示依赖，改为“运行态快照 + 后端事件”收敛
- 补齐前端事件测试、`App` 状态测试与 Rust 自动隐藏单测

**自动化验证**：
- `pnpm test src/__tests__/App.test.tsx src/__tests__/hooks/useSystemEvents.test.tsx src/__tests__/api/events.test.ts` ✅
- `pnpm run type-check` ✅
- `pnpm run lint` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml auto_hides_panel -- --nocapture` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml skips_auto_hide -- --nocapture` ✅

**下一步**：
- 进入 `TASK-01-2`，调整 `macOS` 面板定位逻辑，恢复 `220px` 并覆盖 `Dock`

### 2026-03-08：完成 `TASK-00-1` 建立 `v1.1` 文档基线

**变更类型**：`docs(v1.1)`

**主要产出**：
- 建立 `docs/v1.1/` 全套版本文档
- 锁定 `v1.1` 只聚焦主面板交互与窗口体验修正
- 更新项目决策记录，纠正“避让 Dock”方案

**下一步**：
- 进入 `TASK-01-1`，先收敛失焦自动隐藏与显隐状态同步
