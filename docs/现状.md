根据以下需求,给我一份`软件项目愿景需表`文档,输出到`docs/`文件夹下
```
我需要一个`粘贴板记录管理`小工具
这个工具可以开机自启动
启动后自动监听用户电脑每一次的复制,并且记录复制内容为记录(一段文本,n个图片,n个文件/目录)
在mac下shift + command + v 可以调出粘贴板工具
显示方式:屏幕底部,占满屏幕宽度,记录内容使用横向卡片,可以看到文本内容,图片,文件的预览与图表
按下回车可以粘贴记录,键盘方向键左右可以切换记录,按下delete可以删除记录,按下esc可以关闭
在选中一个记录的时候,shift + 回车 可以把记录以纯文本模式粘贴(去除所有复制的格式只保留文本内容,类似背景色,字体大小,等等都去掉)
`docs/截原型图.jpg` 为原型参考
核心功能可行性评审使用要流畅,性能要好

```

再给我一份`核心功能可行性评审`文档,输出到`docs/`文件夹下

给我一份`模块拆分文档`文档,输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

给我一份`模块界限文档`文档,输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

给我一份`页面地图与页面功能简介文档`文档,输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

给我一份`ui规范文档`文档,输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

给我一份`技术架构选型文档`文档(包括自动化测试,自动化验收),输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

给我一份`项目目录与文件规划文档`文档,输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

给我一份`迭代版本规划文档`文档,输出到`docs/`文件夹下
先参考`docs/`目录下现有的文档,再输出

为v0.1版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v0.1`目录下

为v0.2版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v0.2`目录下

为v0.3版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v0.3`目录下

为v0.5版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v0.5`目录下

为v1.0版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v1.0`目录下


根据`docs/v0.1/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成

根据`docs/v0.2/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成


根据`docs/v0.3/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成


根据`docs/v0.5/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成


根据`docs/v1.0/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成




我希望新增1.1版本,这个版本主要改动为:
- 当粘贴板失去焦点自动隐藏
- 现在粘贴板为了不挡住底部的mac系统dock栏把高度减少了,导致粘贴板下面部分被裁剪看不到了,我想彻底的解决这个问题,粘贴板要能直接盖在dock栏上面,而不是使用减少粘贴板高度的方式解决,恢复粘贴板的原来高度
- command + 数字  可以快速粘贴内容
- 左右选择粘贴板内容的时候,当选择超过可见区域的时候,要自动左右滚动,把不可见的内容显示出来
  为v1.1版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
  注意文档的编写只聚焦于当前版本
  先参考`docs/`目录下现有的文档,再输出到`docs/v1.1`目录下


根据`docs/v1.1/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成


我希望新增1.2版本,这个版本主要改动为:
- 现在卡片的高亮是根据键盘左右选择的,我还需要鼠标单击卡片的时候选中,双击卡片的时候粘贴卡片内容
- 卡片的图片预览现在有bug没有显示图片出来
- 卡片1到9的编号根据可视区域的卡片来,如果卡片左右滚动后,滚动到后面的卡片时无法使用快捷键快捷粘贴
- 设置页的分组tabs改成左右布局
- `当前会话能力完整支持`独立一个分组

为v1.2版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v1.2`目录下


根据`docs/v1.2/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成




我希望新增1.3版本,这个版本主要改动为:
- 选中的卡片,按下空格后,可以放大卡片到屏幕中心去预览卡片的完整内容
- 右键选择卡片并弹出菜单,通过鼠标来完成菜单功能操作
- 优化卡片样式和优化粘贴板面板背景为半透明,我希望ui设计更好看一些,更流行的设计,更科技的设计

为v1.3版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v1.3`目录下


根据`docs/v1.3/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成

以下需求先给我一个方案
Shift+Enter 粘贴纯文本如果是图片,就调用ocr-rs来进行ocr图片中的文字,再粘贴,如果是粘贴图片中的纯文本,则不着急隐藏粘贴板面板,在指定的卡片中先显示"识别文字中",识别完成后再隐藏面板和粘贴识别出来的文字
要考虑打包ocr-rs的方案,直接把ocr-rs嵌入到包内,无需再另外安装依赖




我希望新增1.4版本,这个版本主要改动为:
- 粘贴板面板的`最近记录支持托盘 设置与关于页的发布版基础能力`这些文字元素去掉把,没什么用,还占空间
- 粘贴板面板的背景改为半透明类似毛玻璃的效果,预览的窗口的背景也改成毛玻璃半透明,设置页也改为毛玻璃半透明,关于页也改成毛玻璃半透明
- 现在只有文字,图片,文件的分类,我希望新增视频,超链接,音频,文稿文件的分类,自动识别粘贴板的类型
- 新增搜索功能,可以模糊搜索所有卡片的内容,然后进行筛选
- 把原来粘贴板面板的`最近记录支持托盘 设置与关于页的发布版基础能力`去掉后,改成放搜索框,搜索框横向居中在面板中
- `关于`窗口里面的多余的内容太多了,精简掉,并且把关于的入口从粘贴板面板去掉,就是粘贴板面板右边的关于按钮入口
- 粘贴板面板的左侧添加一个竖线的菜单栏作为类型的快捷筛选:全部,文本,图片,文件,超链接,视频,音频,文稿
- 如果是文本预览的话,我希望加上代码高亮,搜索,替换的功能
- 粘贴板底部的快捷键提示文字,在宽屏下分布的太宽了,影响阅读,我希望居中并且设置一个最大宽度
- 在多屏下,如果我在副屏激活了粘贴板面板,并且使用预览,现在预览只会固定显示在主屏,我希望预览窗口显示在主屏还是副屏要根据粘贴板面板在哪个屏幕激活的来定位
- 把卡片中的时间,放到卡片底部与xxx字符同一行,并且居中,颜色改为和xxx字符一样
- 每种类型的卡片对应一个颜色,包括卡片的标题色和卡片的背景色
- 卡片的标题栏高度增高20px,并且在卡片标题栏的右边新增应用图片,就是这段内容是从哪个应用复制的,就用哪个应用的图标
- 搜索框有个默认最小宽度,我希望搜索框能根据输入的内容的长度来动态改变宽度,并且要有清空icon按钮在搜索框内
- 工具的整体风格ui优化,要类似新版苹果macos的ui风格,包括粘贴板面板,卡片,和所有窗口的ui


为v1.4版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v1.4`目录下


根据`docs/v1.4/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成




我希望新增1.5版本,这个版本主要改动为:
- 新增音效,当新增了复制内容时就播放`docs/音效/复制的音效.mp3`
- 新增音效,当粘贴了内容时就播放`docs/音效/粘贴的音效.mp3`
- 新增音效,当显示预览内容就播放`docs/音效/显示预览的音效.mp3`
- 音效复制到项目里面改为英文名字再使用
- 预览优化一下,当预览窗口显示的时候,当切换选择卡片的时候,预览窗口自动切换为显示对应卡片的内容
- 新增音频的预览功能支持(播放音频),注意,当前项目已有预览窗口,功能的修改在原有的预览窗口上修改
- 新增视频的预览功能支持(播放视频),注意,当前项目已有预览窗口,功能的修改在原有的预览窗口上修改
- 新增PDF,DOC/DOCX,EXCEL,PPT文稿的预览功能支持(显示文稿内容),注意,当前项目已有预览窗口,功能的修改在原有的预览窗口上修改
- 新增超链接的预览功能支持(显示超链接内容),注意,当前项目已有预览窗口,功能的修改在原有的预览窗口上修改


为v1.5版本,编写当前版本的`用户故事文档`+`DDD战术设计文档`+`技术可行性评审文档`+`数据库设计文档`+`自动化测试文档`+`BDD自动化验收文档`+`API契约文档`+`状态码契约文档`+`种子数据契约文档`+`文件目录结构设计文档`+`页面地图与每个页面的详细功能描述文档`+`ux设计文档`+`冲刺任务文档`
注意文档的编写只聚焦于当前版本
先参考`docs/`目录下现有的文档,再输出到`docs/v1.5`目录下


根据`docs/v1.5/冲刺任务文档.md`文档进行开发任务,并且你完成任务后要更新冲刺任务文档,方便我监控进度
先领取未完成的任务->阅读相关文档或者代码->进行任务->任务自动化测试与自动化验收通过后->更新`冲刺任务文档.md`文档->提交代码->领取下一个任务->重复以上步骤直到任务全部完成


