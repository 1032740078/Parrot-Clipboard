# v0.2 冲刺任务文档

> 更新时间：2026-03-06  
> 当前阶段：v0.2 Alpha 已完成  
> 版本目标：图片/文件支持 + SQLite 持久化 + 纯文本粘贴

## 1. 进度总览仪表盘

### 1.1 状态图例

| 状态 | 含义 |
|------|------|
| ✅ 已完成 | 已落地并可验收 |
| 🟡 待开始 | 已定义，尚未进入开发 |
| 🔵 进行中 | 开发中 |

### 1.2 整体进度统计

| 指标 | 数值 |
|------|------|
| 总任务数 | 18 |
| 已完成 | 18 |
| 进行中 | 0 |
| 待开始 | 0 |
| 完成度 | 100% |

### 1.3 批次进度概览

| 批次 | 名称 | 状态 | 完成情况 |
|------|------|------|----------|
| 第 0 批 | 文档基线 | ✅ 已完成 | 1/1 |
| 第 1 批 | 数据与配置基建 | ✅ 已完成 | 4/4 |
| 第 2 批 | 多类型采集与图片处理 | ✅ 已完成 | 4/4 |
| 第 3 批 | 粘贴与 IPC 扩展 | ✅ 已完成 | 3/3 |
| 第 4 批 | 主面板 UI 扩展 | ✅ 已完成 | 3/3 |
| 第 5 批 | 自动化测试与验收 | ✅ 已完成 | 3/3 |

---

## 2. 主任务追踪表

| ID | 批次 | 任务 | 模块 | 依赖 | 预估 | 状态 | 验收摘要 |
|----|------|------|------|------|------|------|----------|
| TASK-00-1 | 第 0 批 | 建立 v0.2 设计与契约文档基线 | 文档 | — | 0.5 天 | ✅ 已完成 | `docs/v0.2` 文档齐备 |
| TASK-01-1 | 第 1 批 | 扩展配置模型，加入三类记录上限 | M09 | TASK-00-1 | 0.5 天 | ✅ 已完成 | `config.json` 支持文本/图片/文件上限 |
| TASK-01-2 | 第 1 批 | 实现 SQLite 迁移与连接管理 | M02 | TASK-00-1 | 1 天 | ✅ 已完成 | 三张业务表与索引创建成功 |
| TASK-01-3 | 第 1 批 | 实现仓储层摘要/详情查询 | M02 | TASK-01-2 | 1 天 | ✅ 已完成 | 支持混合记录读取 |
| TASK-01-4 | 第 1 批 | 实现按类型清理与孤立资源扫描 | M02/M07 | TASK-01-3 | 0.5 天 | ✅ 已完成 | 超限与资源补偿清理可运行 |
| TASK-02-1 | 第 2 批 | 扩展监听层识别图片类型 | M01/M14 | TASK-01-2 | 1 天 | ✅ 已完成 | 图片监听入库与事件触发已打通 |
| TASK-02-2 | 第 2 批 | 扩展监听层识别文件/目录类型 | M01/M14 | TASK-01-2 | 0.75 天 | ✅ 已完成 | 文件/目录监听入库与顺序保持已打通 |
| TASK-02-3 | 第 2 批 | 实现图片原图保存与缩略图生成 | M07 | TASK-02-1 | 1 天 | ✅ 已完成 | 原图保存、pending/ready/failed 状态更新已打通 |
| TASK-02-4 | 第 2 批 | 实现三种类型统一去重与置顶 | M01/M02 | TASK-02-1,TASK-02-2 | 0.75 天 | ✅ 已完成 | 文本/图片/文件命中旧记录时均可复用 ID 并置顶 |
| TASK-03-1 | 第 3 批 | 扩展 `paste_record` 支持图片与文件 | M06 | TASK-02-3,TASK-02-2 | 1 天 | ✅ 已完成 | 文本/图片/文件三种类型均可再次粘贴 |
| TASK-03-2 | 第 3 批 | 实现纯文本粘贴模式 | M06 | TASK-03-1 | 0.5 天 | ✅ 已完成 | 文本记录可用 `plain_text` 模式写回纯文本 |
| TASK-03-3 | 第 3 批 | 扩展 IPC Command/Event 契约 | M13 | TASK-01-3,TASK-03-1 | 0.75 天 | ✅ 已完成 | 摘要/详情/更新事件与兼容层打通 |
| TASK-04-1 | 第 4 批 | 扩展前端类型与 Store | M12 | TASK-03-3 | 0.75 天 | ✅ 已完成 | 混合记录摘要、Store 动作与更新事件已打通 |
| TASK-04-2 | 第 4 批 | 实现 `ImageCard` / `FileCard` / 骨架态 | M05 | TASK-04-1 | 1 天 | ✅ 已完成 | 图片/文件/骨架卡片与混合列表渲染正常 |
| TASK-04-3 | 第 4 批 | 扩展主面板键盘行为与错误提示 | M05 | TASK-04-2,TASK-03-2 | 0.75 天 | ✅ 已完成 | 纯文本模式、禁用提示与错误 Toast 已打通 |
| TASK-05-1 | 第 5 批 | 补齐前后端单元与集成测试 | 全局 | 第 4 批全部 | 1 天 | ✅ 已完成 | 前后端单元/契约测试缺口已补齐并全量通过 |
| TASK-05-2 | 第 5 批 | 建立 BDD / E2E 自动化场景 | 全局 | TASK-05-1 | 1 天 | ✅ 已完成 | 文本/图片/文件三条主流程已可自动验收 |
| TASK-05-3 | 第 5 批 | 完成性能与稳定性回归 | 全局 | TASK-05-2 | 0.5 天 | ✅ 已完成 | 冷启动、缩略图、坏库恢复回归已自动化达标 |

---

## 3. 各批次任务详情

### 3.1 第 0 批：文档基线

**目标**：明确 v0.2 范围、领域模型、接口、数据库与验收口径。

- `TASK-00-1` 已完成：输出用户故事、DDD、数据库、API、状态码、种子数据、页面地图、UX、测试与冲刺文档

### 3.2 第 1 批：数据与配置基建

**目标**：先把可持久化的数据底座搭稳。

**完成标准**：
- `config.json` 扩展三类上限配置
- SQLite 数据库可自动迁移与重建
- 主面板可读取最近 100 条混合摘要
- 超限清理与孤立资源清理可独立运行

### 3.3 第 2 批：多类型采集与图片处理

**目标**：让监听层真正接住图片与文件，并把图片资源保存完整。

**完成标准**：
- 监听到图片后可保存原图并异步生成缩略图
- 监听到文件/目录后可保存路径集合
- 文本、图片、文件统一走去重与置顶策略

### 3.4 第 3 批：粘贴与 IPC 扩展

**目标**：打通“历史记录再次粘贴”与“前端读写接口”。

**完成标准**：
- `paste_record` 支持三种类型
- `get_record_detail` 可读取完整详情
- `clipboard:record-updated` 可驱动缩略图刷新与记录置顶

### 3.5 第 4 批：主面板 UI 扩展

**目标**：让主面板从单一文本列表升级为混合记录浏览器。

**完成标准**：
- 新增图片卡片、文件卡片、骨架态
- 保留 v0.1 键盘优先体验
- 非文本卡片的纯文本操作有明确禁用反馈

### 3.6 第 5 批：自动化测试与验收

**目标**：确保新增加的持久化与多类型能力可回归、可验收。

**完成标准**：
- Must 级用户故事有自动化测试映射
- E2E 能覆盖文本、图片、文件三条主链路
- 关键性能指标达标

---

## 4. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 图片格式兼容性不足 | 图片记录与粘贴失败 | 先聚焦 PNG/JPEG/GIF，其他格式记录日志降级 |
| SQLite 与文件系统状态不一致 | 历史残缺或资源泄漏 | 建立事务 + 启动补偿清理 |
| 自动化桌面测试不稳定 | CI 波动 | 将高频单测与少量关键 E2E 分层执行 |
| 纯文本模式边界多 | 富文本剥离异常 | 优先对 HTML/RTF 建立回归用例 |

---

## 5. 冲刺完成后建议

1. `v0.2 Alpha` 冲刺任务已全部完成，可进入发布前收敛阶段
2. 下一步优先做真实设备长稳回归、打包验收与发布说明整理
3. 若继续扩展版本范围，再规划搜索、设置页或跨端同步等非本期能力

---

## 6. 最新进展

### 2026-03-06：完成 `TASK-05-3` 性能与稳定性回归

**状态**：✅ 已完成

**完成内容**：
- 更新 `src-tauri/src/persistence/mod.rs`，新增损坏 SQLite 数据库的自动备份与重建逻辑，应用启动遇到坏库时可恢复进入空库状态
- 更新 `src-tauri/src/persistence/sqlite.rs` 与 `src-tauri/src/image/storage.rs`，新增最近 100 条摘要读取 `< 80ms`、常见窗口截图缩略图 `< 50ms` 的 Rust 回归测试，并优化 PNG 缩略图生成路径
- 更新 `vitest.config.ts`，排除 `e2e/**`，确保前端覆盖率与 Playwright 场景分层执行且互不干扰

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run test:e2e` ✅ 通过

**下一任务**：
- `v0.2 Alpha` 冲刺任务已全部完成

### 2026-03-06：完成 `TASK-05-2` 建立 BDD / E2E 自动化场景

**状态**：✅ 已完成

**完成内容**：
- 新增 `e2e/playwright.config.ts`、`e2e/tests/bdd.spec.ts` 与 `e2e/support/tauriMock.js`，搭建基于 Playwright 的 BDD / E2E 自动化场景与 Tauri mock 运行时
- 更新 `package.json`，新增 `pnpm run test:e2e` 命令，并安装 `@playwright/test` 依赖与 Chromium 浏览器
- 完成文本纯文本粘贴、图片原格式粘贴、文件原格式粘贴三条 Must 主链路的自动化验收脚本

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:e2e` ✅ 通过

**下一任务**：
- 领取 `TASK-05-3`：完成性能与稳定性回归

### 2026-03-06：完成 `TASK-05-1` 补齐前后端单元与集成测试

**状态**：✅ 已完成

**完成内容**：
- 新增 `src/__tests__/api/errorHandler.test.ts`，补齐前端错误码解析与统一提示文案测试，对齐 `UT-FE-API-002`
- 更新 `src/__tests__/components/MainPanel.test.tsx`，补齐图片记录原格式粘贴、非文本纯文本禁用、图片记录删除等主面板交互测试场景
- 重新跑通前端覆盖率测试与后端 `cargo test` / `clippy`，确认当前前后端单元、契约、组件、Hook 与后端核心测试全部通过

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

**下一任务**：
- 领取 `TASK-05-2`：建立 BDD / E2E 自动化场景

### 2026-03-06：完成 `TASK-04-3` 扩展主面板键盘行为与错误提示

**状态**：✅ 已完成

**完成内容**：
- 更新 `src/hooks/useKeyboard.ts`，补齐 `Shift+Enter` 纯文本粘贴、非文本禁用拦截、删除失败提示与成功后同步关闭前端面板状态
- 更新 `src/stores/useUIStore.ts`、`src/components/common/Toast.tsx` 与 `src/App.tsx`，将 Toast 提示接入全局 UI Store，支持 info/error 两类提示与自动消失
- 更新 `src/components/MainPanel/index.tsx`，新增底部快捷键提示栏，并在非文本选中时弱化 `Shift+Enter` 提示
- 补充键盘 Hook、主面板与 UI Store 自动化测试，覆盖纯文本模式、禁用提示、失败 Toast 与快捷键提示态

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

**下一任务**：
- 领取 `TASK-05-1`：补齐前后端单元与集成测试

### 2026-03-06：完成 `TASK-04-2` 实现 `ImageCard` / `FileCard` / 骨架态

**状态**：✅ 已完成

**完成内容**：
- 新增 `src/components/MainPanel/ImageCard.tsx`、`FileCard.tsx`、`SkeletonCard.tsx`，补齐图片缩略图占位态、文件摘要卡片与首次加载骨架态
- 更新 `src/components/MainPanel/CardList.tsx` 与 `src/components/MainPanel/index.tsx`，主面板现在可按记录类型渲染文本/图片/文件卡片，并在首次加载时展示骨架列表
- 补充 `ImageCard`、`FileCard`、`SkeletonCard` 与主面板混合渲染测试，覆盖图片 ready/pending、文件数量摘要与加载态场景

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

**下一任务**：
- 领取 `TASK-04-3`：扩展主面板键盘行为与错误提示

### 2026-03-06：完成 `TASK-04-1` 扩展前端类型与 Store

**状态**：✅ 已完成

**完成内容**：
- 更新 `src/types/clipboard.ts`，将前端记录类型切换为 v0.2 混合摘要模型，并补充预览文本与类型守卫工具
- 更新 `src/stores/useClipboardStore.ts`，新增 `hydrate`、`upsertRecord`、`updateRecord`、`markThumbnailReady`、`isHydrating` 等动作与状态，支持混合记录排序与更新
- 更新 `src/hooks/useClipboardEvents.ts`、`src/hooks/useKeyboard.ts`、`src/components/MainPanel/index.tsx`，让主面板改为消费摘要 DTO、监听 `clipboard:record-updated` 并保持置顶/缩略图刷新能力
- 补充前端 Store / Hook / API 自动化测试，覆盖混合记录 hydrate、图片缩略图更新、键盘粘贴与主面板初始化场景

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

**下一任务**：
- 领取 `TASK-04-2`：实现 `ImageCard` / `FileCard` / 骨架态

### 2026-03-06：完成 `TASK-03-3` 扩展 IPC Command/Event 契约

**状态**：✅ 已完成

**完成内容**：
- 更新 `src/api/types.ts`，补齐 v0.2 摘要/详情/粘贴结果/监听状态与更新事件 DTO
- 新增 `src/api/recordAdapters.ts`，统一兼容旧 `text_content` 结构与新 `preview_text` / `evicted_ids` 结构，避免旧 UI 在 v0.2 契约下崩溃
- 更新 `src/api/commands.ts`、`src/api/events.ts` 与前端 API 测试，打通 `get_record_detail`、`paste_record`、`get_monitoring_status`、`clipboard:record-updated` 等 IPC 契约，同时兼容旧 Hook / 旧 mock

**自动化验证**：
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

**下一任务**：
- 领取 `TASK-04-1`：扩展前端类型与 Store

### 2026-03-06：完成 `TASK-03-2` 实现纯文本粘贴模式

**状态**：✅ 已完成

**完成内容**：
- 补充文本记录 `plain_text` 模式测试，验证纯文本粘贴仅调用 `write_text`，不会写入富文本格式
- 调整 `text_strip` 逻辑，补充换行归一化，保证纯文本模式输出稳定
- 继续保留对非文本记录的 `plain_text` 拒绝校验，对应纯文本模式后端约束已完整落地

**自动化验证**：
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过

**下一任务**：
- 领取 `TASK-03-3`：扩展 IPC Command/Event 契约

### 2026-03-06：完成 `TASK-03-1` 扩展 `paste_record` 支持图片与文件

**状态**：✅ 已完成

**完成内容**：
- 为 `PasteService` 补充图片与文件粘贴测试，验证图片记录会恢复完整图片字节、文件记录会按原顺序写回系统文件列表
- 测试桩扩展为可记录真实写入的图片数据和文件路径集合，对应 `UT-PASTE-003` / `UT-PASTE-004` 已自动化覆盖
- 第 3 批粘贴链路已从文本扩展到三种记录类型，为后续纯文本模式和 IPC 收口打下基础

**自动化验证**：
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过

**下一任务**：
- 领取 `TASK-03-2`：实现纯文本粘贴模式

### 2026-03-06：完成 `TASK-02-4` 实现三种类型统一去重与置顶

**状态**：✅ 已完成

**完成内容**：
- 补充混合类型复用测试，验证文本、图片、文件三类记录命中旧内容时都会复用原 ID 而不是生成新记录
- 补充监听层 `promoted` 事件测试，确保记录被复用后会通过 `clipboard:record-updated` 推送置顶刷新
- 第 2 批“多类型采集与图片处理”全部完成，图片/文件监听、图片资源处理、统一去重与置顶策略已全部收口

**自动化验证**：
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过

**下一任务**：
- 领取 `TASK-03-1`：扩展 `paste_record` 支持图片与文件

### 2026-03-06：完成 `TASK-02-3` 实现图片原图保存与缩略图生成

**状态**：✅ 已完成

**完成内容**：
- 图片记录改为先以 `thumbnail_state=pending` 入库并保存原图文件，符合 `UT-DB-003` 与 `BDD-01-01` 的占位态要求
- 监听层新增后台缩略图处理与 `clipboard:record-updated` 事件通知，缩略图生成成功/失败后会分别发送 `thumbnail_ready` / `thumbnail_failed`
- 图片存储模块补充 200×200 缩略图生成、损坏原图失败降级、资源删除清理等测试，图片资源链路已完整收口

**自动化验证**：
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过

**下一任务**：
- 领取 `TASK-02-4`：实现三种类型统一去重与置顶

### 2026-03-06：完成 `TASK-02-2` 扩展监听层识别文件/目录类型

**状态**：✅ 已完成

**完成内容**：
- 运行时仓储补充文件记录入库测试，验证文件列表顺序、目录识别、摘要统计与重复文件集合复用逻辑
- 监听层补充文件优先级测试，确保 Finder 风格文件列表会优先于图片/文本被识别并进入 `capture_files` 链路
- 第 2 批的图片/文件两条监听主链路均已具备自动化覆盖，可继续推进图片资源处理与统一去重置顶

**自动化验证**：
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过

**下一任务**：
- 领取 `TASK-02-3`：实现图片原图保存与缩略图生成

### 2026-03-06：完成 `TASK-02-1` 扩展监听层识别图片类型

**状态**：✅ 已完成

**完成内容**：
- 监听服务改为基于 `ClipboardSnapshot` 识别图片快照，支持图片优先于文本的读取顺序与签名去重
- 新增运行时仓储 `capture_image` 链路，图片复制后可写入 SQLite 摘要/详情，并落地原图与缩略图状态
- 补充图片监听与重复图片复用单测，对应 `BDD-01-01` 的后端核心链路已具备自动化覆盖

**自动化验证**：
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` ✅ 通过
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- `pnpm run lint` ✅ 通过
- `pnpm run type-check` ✅ 通过
- `pnpm run test:coverage` ✅ 通过

**下一任务**：
- 领取 `TASK-02-2`：扩展监听层识别文件/目录类型

### 2026-03-06：完成 `TASK-01-4` 实现按类型清理与孤立资源扫描

**状态**：✅ 已完成

**完成内容**：
- SQLite 层新增按类型超限清理能力，只淘汰同类型最旧记录，并为图片记录返回待删除原图/缩略图路径
- 补充孤立图片资源扫描逻辑，可独立比对 `image_assets` 与磁盘目录，识别无数据库引用的原图和缩略图
- 第 1 批“数据与配置基建”全部完成，SQLite 数据底座具备初始化、读取、清理三项基础能力

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- 新增按类型清理、图片资源返回、孤立文件扫描等用例

**下一任务**：
- 领取 `TASK-02-1`：扩展监听层识别图片类型

### 2026-03-06：完成 `TASK-01-3` 实现仓储层摘要/详情查询

**状态**：✅ 已完成

**完成内容**：
- 新增后端 `ClipboardRecordSummary` / `ClipboardRecordDetail` 查询 DTO，覆盖文本、图片、文件三类摘要与详情负载
- 为 SQLite 层补充 `row_mapper` 与混合类型查询逻辑，支持主面板摘要列表与单条详情读取
- 扩展 `ContentType` / `PasteMode` 枚举，补齐 `image`、`files` 与 `plain_text` 契约基础类型

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- 新增混合摘要排序、类型详情读取、缺失记录返回 `None` 等用例

**下一任务**：
- 领取 `TASK-01-4`：实现按类型清理与孤立资源扫描

### 2026-03-06：完成 `TASK-01-2` 实现 SQLite 迁移与连接管理

**状态**：✅ 已完成

**完成内容**：
- 新增 `persistence/` 模块，落地 `clipboard.db` 运行时路径解析、SQLite 连接管理与启动初始化
- 采用 `PRAGMA user_version` 管理迁移版本，按 `v1=三张业务表`、`v2=索引` 顺序自动迁移
- 启动阶段接入数据库初始化，并补充 `DB_ERROR` 错误码映射，为后续仓储层接入做好准备

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过
- 新增 `UT-DB-001` / 幂等初始化用例，覆盖三张表、四个索引与 `user_version=2`

**下一任务**：
- 领取 `TASK-01-3`：实现仓储层摘要/详情查询

### 2026-03-06：完成 `TASK-01-1` 扩展配置模型，加入三类记录上限

**状态**：✅ 已完成

**完成内容**：
- `AppConfig` 新增 `max_image_records`、`max_file_records`，默认值对齐文档约束：文本 `200`、图片 `50`、文件 `100`
- 应用启动改为从运行时 `config.json` 加载配置；首次启动自动创建默认配置文件
- 兼容旧版仅包含 `max_text_records` 的配置文件，启动时自动补齐新字段并回写

**自动化验证**：
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

**下一任务**：
- 领取 `TASK-01-2`：实现 SQLite 迁移与连接管理
