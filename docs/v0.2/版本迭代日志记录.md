# v0.2 版本迭代日志记录

> 更新时间：2026-03-06
> 分支：`main`
> 当前阶段：v0.2 Alpha 第 1 批开发中（2/18 任务完成）

---

## 1. 迭代总览

| 版本标记 | 日期 | 状态 | 完成度 | 说明 |
|---|---|---|---|---|
| v0.2.0-alpha.1 | 2026-03-06 | 已完成 | 2/18 | 完成 `TASK-01-1`，配置模型扩展到文本/图片/文件三类上限，并接入运行时 `config.json` 自动创建与兼容迁移 |
| v0.2.0-doc.1 | 2026-03-06 | 已完成 | 1/18 | 建立 v0.2 全套设计与契约文档，锁定当前版本范围、数据库方案、接口与验收基线 |

---

## 2. 本次变更明细

### 2.1 2026-03-06 `task-01-1-config-retention`

**变更类型**：`feat(config)`

**主要变更**：
- 更新 `src-tauri/src/config/schema.rs`，新增 `max_image_records`、`max_file_records` 字段，并将默认保留上限调整为文本 `200`、图片 `50`、文件 `100`
- 更新 `src-tauri/src/config/mod.rs`，新增运行时 `config.json` 的加载、首次创建与旧配置自动补齐回写逻辑
- 更新 `src-tauri/src/lib.rs`，应用启动阶段改为加载真实配置文件，再注入全局状态与快捷键模块
- 补充 Rust 单元测试，覆盖默认值、旧配置兼容迁移、配置文件自动创建场景

**产出结果**：
- `v0.2` 的三类记录上限配置已真正落地到运行时配置文件
- 从 `v0.1` 升级到 `v0.2` 时，不需要手动删除旧配置文件即可平滑补齐新字段
- 第 1 批数据与配置基建进入进行中状态，为后续 SQLite 持久化任务提供配置基础

**验证结果**：
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 通过

### 2.2 2026-03-06 `working-tree`

**变更类型**：`docs(v0.2)`

**主要变更**：
- 新增 `docs/v0.2/用户故事文档.md`
- 新增 `docs/v0.2/DDD战术设计文档.md`
- 新增 `docs/v0.2/数据库设计文档.md`
- 新增 `docs/v0.2/自动化测试文档.md`
- 新增 `docs/v0.2/BDD自动化验收文档.md`
- 新增 `docs/v0.2/API契约文档.md`
- 新增 `docs/v0.2/状态码契约文档.md`
- 新增 `docs/v0.2/种子数据契约文档.md`
- 新增 `docs/v0.2/文件目录结构设计文档.md`
- 新增 `docs/v0.2/页面地图与每个页面的详细功能描述文档.md`
- 新增 `docs/v0.2/UX设计文档.md`
- 新增 `docs/v0.2/冲刺任务文档.md`

**产出结果**：
- 明确 `v0.2` 只聚焦图片/文件支持、SQLite 持久化、纯文本粘贴与主面板扩展
- 统一了数据模型、API 契约、错误码、测试边界与冲刺任务拆解
- 为后续开发实现提供稳定文档基线

---

## 3. 当前版本结论

### 已锁定事项

- 文本、图片、文件三类记录共享主表，类型特有数据分表存储
- 图片原图与缩略图采用“数据库元数据 + 文件系统资源”的组合模式
- 主面板仍为唯一核心界面，不引入设置页、托盘与搜索页
- 纯文本粘贴仅对文本记录开放

### 下一步建议

1. 优先实现 SQLite 迁移与仓储层
2. 紧接着实现图片/文件监听与资源保存
3. 然后扩展粘贴链路与主面板 UI
4. 最后补齐自动化测试与 E2E 验收
